# Open Questions
<!-- status: final -->

Anything that would otherwise become "we'll figure it out later." Every entry
needs an owner and a decide-by date. This register is what stops the exit
criteria being silently dodged.

Rules:
- A `verdict: unknown` on a deep project belongs here.
- An unresolved conflict between two projects' designs belongs here.
- Closing an entry means linking the ADR or document that resolved it.

## Status key

`open` · `investigating` · `resolved` · `deferred` (with a reason and a revisit date)

---

## Register

| ID | Question | Raised by | Owner | Decide by | Status | Resolution |
|---|---|---|---|---|---|---|
| OQ-001 | Does the LLM gateway (`freellm.kaapibyte.com`) have a durable provider-key configuration, or will availability keep flapping? First probe returned `no_providers_configured`, second succeeded. Affects any tooling that depends on it. | Phase 0 setup | | | closed 2026-08-30 — tooling of the study; no claim depends on it | Non-blocking: no teardown claim may come from a model. |
| OQ-002 | Several shortlist projects may be announcement-only with no readable source. Need substitutes that answer the same architectural question. | Phase 0 planning | | 2026-08-26 | resolved | Phase 1 recon: Google AX (`google/ax`) and Omnigent (`omnigent-ai/omnigent`) are both class A and highly relevant. GroupMind and Agent Control could not be located — downgraded to `recon`, questions reassigned in `synthesis/recon.md`. |
| OQ-003 | G-section (human-agent workspace) had the thinnest expected evidence after GroupMind could not be located. | Phase 1 recon | | Phase 5 | **partly resolved** | Letta supplies far more than expected: Slack/Discord/Telegram channels with allowed/admin user lists, `dm\|group` scope, `allow\|deny\|pair` access decisions, per-channel permission modes, mention-only mode, thread context, and durable approvals delivered to whichever channel the human is on. G-section now has a strong primary source. Still worth confirming with Omnigent's session sharing. |
| OQ-004 | Letta's source moved to `letta-ai/letta-code`; is the memory architecture still inspectable or has it moved behind Letta Cloud? | Phase 1 recon | | Phase 2 | **resolved** | Fully inspectable, and better than the V1 block store would have been: memory is a git-backed markdown filesystem with kernel-enforced per-agent isolation. Produced the first `first_class` H4 in the study. ADR-0008 confirmed strongly. |
| OQ-005 | Four projects (LangGraph/LangSmith, Letta, AWS AgentCore, Google Agent Platform) keep their control plane closed — exactly the layer we intend to build. Prior art for identity, hosted memory, multi-tenancy and observability backends is thin across the whole study. | Phase 1 recon | | Phase 5 | closed 2026-08-30 — structural limit of the exercise, already weighted | Structural limit of the exercise. Weight Google AX and Omnigent more heavily; both keep their control plane open. |
| OQ-006 | `web_search` / `web_fetch` were unavailable during recon (no local Ollama) and the GitHub search API was IP-rate-limited, so recon relied on direct repo-path probing. A project could have been missed. | Phase 1 recon | | Phase 2 | resolved | **Re-checked 2026-08-26 via npm + PyPI registries.** Agent Control located at `agentcontrol/agent-control` (Apache-2.0, 299★) and restored to `targeted`. GroupMind confirmed absent across GitHub, npm and PyPI. Lesson: package registries beat GitHub search for discovery, since they carry the canonical repo URL. |
| OQ-007 | Does LangGraph Platform add definition-version pinning on resume, or does it inherit the silent work-loss hazard proven in OSS (renamed node → resume returns empty, no error)? Platform is closed source. | LangGraph teardown | | Phase 5 | closed 2026-08-30 — class-B evidence only (closed source); ADR-0011 stands on the OSS proof | Docs review only; class B evidence at best. Relates to ADR-0011. |
| OQ-008 | What lease/heartbeat mechanism does LangGraph Platform use to detect lost workers? Closed source, so `C8` stayed `unknown`. | LangGraph teardown | | Phase 5 | **partly resolved** | Letta answers the general question well: `SchedulerOwner` with pid + token + `process_start_ticks` + `boot_id` detects stale ownership across PID reuse and reboot (`src/cron/cron-file.ts:45-52 @ 852ca24`). Adopt that pattern; LangGraph's specific mechanism remains unknown. |
| OQ-009 | Is there any accepted pattern for side-effect idempotency across a resume boundary, or is developer discipline the entire answer? | LangGraph teardown | | Phase 5 | closed 2026-08-30 — answered: no idempotency pattern exists; the effect ledger is the answer (ADR-0014) | **Six for six: no project has an idempotency key on tool calls.** Google AX is closest structurally (atomic per-step log appends, idempotent `CreateActor`); Omnigent found and fixed an adjacent lost-update race on usage deltas (`BEGIN IMMEDIATE` / `SELECT FOR UPDATE`, bug #9) but leaves tool effects unprotected. This is now confirmed as a genuine industry gap and a BUILD item: a durable effect-ledger keyed on a deterministic task id. |
| OQ-010 | Does the OpenHands agent-server define crash-recovery semantics for a conversation interrupted mid-action, or is the event log the only contract? | OpenHands teardown | | Phase 3 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | `C5` left `unknown` for both OpenHands and Letta. |
| OQ-011 | How does OpenHands Cloud enforce the quotas the OSS Docker workspace lacks (no CPU/memory/pid limits)? Closed. | OpenHands teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Relevant to S9, which is unanswered in all three deep projects. |
| OQ-012 | Is ACP's stdio transport sufficient for a genuinely remote agent, or does it force co-location? | OpenHands teardown | | Phase 3 | **resolved** | Google AX answers it: `HarnessService.Connect` is a gRPC bidirectional stream, so a remote harness needs **no co-location** (`proto/ax.proto:85-91 @ b777313`). Implication: our southbound API should be gRPC-shaped with ACP as one adapter *behind* it, not as the transport. Recorded as an amendment on ADR-0006. |
| OQ-013 | Does any project have agent versioning or revocation? | Letta teardown | | Phase 3 | **partly resolved** | **Versioning: yes, once.** Omnigent has a monotonic `Agent.version` incremented on every bundle update over a content-addressed tarball (`stores/agent_store/sqlalchemy_store.py:300 @ ba9e371`) — the only agent versioning in six projects. **Revocation: still absent six for six** — Omnigent hard-deletes agents, though its *device grants* have a real revocation lifecycle. Combined with OQ-020, the lesson is that versioning without a run-side pin does not prevent mid-flight upgrade. |
| OQ-014 | How does git-backed memory behave at scale — repository growth, history rewriting, conflict frequency under unattended operation? | Letta teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Affects whether we borrow the pattern or only the ideas. |
| OQ-015 | Is Letta's `pair` (pending sender authorisation) durable across restart like `PendingControlRequest`, or in-memory only? | Letta teardown | | Phase 3 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Minor, but the pattern is one we want to copy. |
| OQ-016 | **S9 (runaway spend) is `undefined` or partial in all three deep projects.** None has a hard cost ceiling; the best available are goal-loop `capped` status (OpenHands) and subagent context budgets (Letta). No infrastructure-level quota anywhere. | Phase 2 synthesis | | Phase 5 | merged 2026-08-30 into OQ-098 | Likely a genuine differentiation opportunity. Budget enforcement may need to be a control-plane concern rather than a runtime one. |
| OQ-017 | Does AX intend `Interaction` to become a pollable resource (per its own `TODO`), and would that make it a Task or still a Run? | Google AX teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Closest thing to Task/Run separation found so far; the `TODO` suggests even Google finds this hard to land. |
| OQ-018 | Is Google AX's total absence of authN a "not yet" or a deliberate deployment assumption (service mesh / sidecar auth)? Nothing in the repo says. | Google AX teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Affects whether we read it as an oversight or as a valid architectural stance for a control plane behind a mesh. |
| OQ-019 | How does SubstrATE handle actor eviction under memory pressure, and does a suspended actor expire? Determines whether suspend/resume is a durability mechanism or only a cost optimisation. | Google AX teardown | ADR-0016 | Phase 5 | open — parked (2026-08-27: v0.1 provider is gVisor checkpoint-and-kill; revisit only if a provider change is proposed) | External dependency (`github.com/agent-substrate/substrate`), not read. **Now blocks ADR-0016** (sandbox placement), which cannot move from Proposed to Accepted until eviction and expiry semantics are known: if a suspended actor can expire silently, suspend/resume is a cost optimisation and not a durability mechanism, and the data-plane design changes. Probe: read the SubstrATE source. |
| OQ-020 | Does anything pin `Agent.version` onto a conversation at start in Omnigent, or is the version purely an audit counter? | Omnigent teardown | | Phase 5 | **resolved** | Nothing does. Searched `entities/conversation.py` — no version field. This is the ADR-0011 challenge: versioning without pinning. Recorded as an ADR-0011 amendment (the pin lives on the run, not the definition). |
| OQ-021 | Is Omnigent's `harness_bench` DRIFT reconciliation run in CI, or a manual tool? | Omnigent teardown | | Phase 4 | **resolved** | **Two layers, and the split is the answer.** `tests/harness_bench/test_bench.py` runs an **offline** layer on every PR — registry membership, *profile completeness*, reconciliation semantics, community-profile resolution, matrix rendering — with no network or credentials. The **live** probe layer (which produces real DRIFT verdicts) is gated on `--profile` plus a runnable harness CLI, and asserts P0 dimensions match the declaration. So the *structure* is CI-enforced continuously and the *behavioural claims* are enforced only when credentials exist. Adopted into ADR-0012: split our bench the same way, because it means declaration completeness never regresses even when live probes cannot run. |
| OQ-022 | Why are all four optional capability axes (`steering`, `live_queue`, `images`, `compaction`) unpopulated across all 26 Omnigent harnesses — is the tri-state new, or is populating it blocked on live probing? | Omnigent teardown (verified live, 0.11.0) | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | A tri-state nobody fills buys nothing. Drove the ADR-0012 requirement that our bench report *declaration coverage*, not just drift. |
| OQ-023 | Are Omnigent's scheduled task runs claimed with a lease or lock? | Omnigent teardown | | Phase 5 | **resolved** | No. `ScheduledTaskScheduler` is explicitly an in-process RRULE scheduler with "no in-memory schedule state beyond the live timers" (`omnigent/server/scheduled/scheduler.py:1-24`). SKIP overlap and 30s misfire grace guard self-overlap only; **two replicas would double-fire**, and missed fires are deliberately not replayed. Warning for our scale-out control plane: runs need a durable claim (`UPDATE ... WHERE status='scheduled'` on rowcount, or `SELECT ... FOR UPDATE SKIP LOCKED`). |
| OQ-024 | What happens to AG2's dedupe guarantee after terminal-channel pruning clears `_causation_index`? `find_envelope_by_causation` returns `None` "either it was never accepted or its channel has already closed" — conflating "no duplicate" with "cannot tell". | AG2 teardown | | Phase 5 | closed 2026-08-30 — resolved by spike 01; spec/00's table already said so — status was stale | Material to ADR-0014: our effect ledger must not have a retention horizon that silently degrades into "cannot tell". A tombstone or an explicit INDETERMINATE result is needed. |
| OQ-025 | Is there a federation story for WAL consistency across AG2 hubs, given `remote_agent` passports are explicitly "a cache"? | AG2 teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Affects whether our messaging design can span control planes or must assume one authority per channel. |
| OQ-026 | Does AG2's `record_observation` weight recent outcomes? | AG2 teardown | | Phase 5 | **resolved** | No. `ObservedStat` is a **lifetime counter with no decay**, and `p50_latency_ms` "replaces the prior" value — "a single-sample stand-in for a future reservoir", so it is not a percentile at all. `task_id` dedupes double-counting (cascade EXPIRED + owner COMPLETED). Notably, observation **expands** the capability index: an agent appears under a capability "even if it wasn't in their original `claimed_capabilities`", so capabilities can be *discovered*. Gap for us: routing on reputation needs a window or reservoir, since a lifetime counter never forgets an early failure. |
| OQ-027 | Does a durable engine preserve a pending `DeferredToolRequests` across workflow suspension, giving Pydantic AI durable HITL for free? | Pydantic AI teardown | | Phase 5 | **partly resolved** | Found two composable modes rather than one. Default: the run *returns* `DeferredToolRequests` and the caller owns pending state. Alternative: `HandleDeferredToolCalls` resolves inline, and a handler may **decline by returning `None`** so the next handler in the chain tries, with unhandled calls bubbling up as output. That chain-with-fallthrough is worth copying — policy auto-approves what it recognises, humans get the remainder. Whether a durable engine persists the pending value across suspension is still unverified. |
| OQ-028 | Is `genai-prices` maintained on a cadence that makes it safe to depend on for budget *enforcement*, rather than only reporting? | Pydantic AI teardown | | Phase 5 | merged 2026-08-30 into OQ-130 | Relevant if we adopt it for ADR-0013 cost gates. Note Pydantic AI itself only *measures*; Omnigent enforces, and fails closed when a model has no price — that combination (external price data + fail-closed on unpriced) is probably the right design. |
| OQ-029 | Does any ADK session backend enforce `(app_name, user_id)` scoping at the storage layer, or is it a query convention? | Google Agent Platform teardown | | Phase 5 | **resolved** | **Enforced.** `StorageSession` declares `app_name`, `user_id` and `id` all `primary_key=True` (`sessions/schemas/v0.py:146-161 @ 85b52f6`), so a lookup that omits or mistakes the scope finds nothing. Same mechanism as Omnigent's `workspace_id` — **second independent convergence on tenancy-in-the-primary-key**, which settles the approach for our design. Caveat: nothing authorizes the caller's claim to a `user_id`, so this prevents accidental leaks and not deliberate impersonation — which is exactly the principal/credential split now recorded on ADR-0007. Process note: I initially wrote "convention, not enforcement" and had to correct it by reading the schema. |
| OQ-030 | Does Vertex AI Memory Bank support TTL or deletion, given `custom_metadata` names TTL as a service-specific field that "may later become first-class"? | Google Agent Platform teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Relevant to `H6` (knowledge expiry), which is `implicit` at best everywhere. The interface anticipates expiry without specifying it, so our Recall interface should include TTL from the start rather than as metadata. |
| OQ-031 | Does HumanLayer's daemon reconcile approvals on restart — can a tool call whose approval was decided while the daemon was down still proceed? | HumanLayer teardown | | Phase 5 | **resolved** | **Yes, and it is a named abstraction.** `session.ApprovalReconciler` is documented as an "interface for reconciling approvals after session restart", with `ReconcileApprovalsForSession(ctx, runID)` called from three sites in `session/manager.go` (:522, :1849, :2067). On session start it waits 2s (cancellable) for the session to come up, then reconciles by `run_id`, and **logs on failure rather than failing the session**. That fail-open choice is sound *because* the approvals are durable: a failed reconcile leaves the pending row intact for the next attempt, whereas failing the session would strand work. Shape adopted into ADR-0015 rule 1: reconciliation keyed on the run, triggered by restart, non-fatal. |
| OQ-032 | Does HumanLayer's hosted (commercial) service record an approver identity, making the audit gap a local-only limitation? | HumanLayer teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | The OSS daemon's `approvals` table has no `decided_by`. If the hosted service adds one, the gap is a single-user artefact rather than a design position — but either way **our** approval must name its principal (ADR-0015 rule 4). Not blocking: the decision is made on requirements, not on their precedent. |
| OQ-033 | Does AgentCore's Cedar integration expose policy *analysis* (equivalence, permissiveness comparison), or only evaluation? | AWS AgentCore teardown | | Phase 5 | **resolved** | **Tested, and the answer is partial.** Installed `cedarpy` and ran it: `validate_policies` catches a typo'd attribute name statically (`resource.riskLevel` vs `resource.risk` → `validation_passed=False`), which is the strongest argument for Cedar and better than the one I originally made. But **equivalence / implication / permissiveness comparison are not exposed in the Python binding** — they live in Cedar's Rust/Lean analysis crate. So **static** shadow comparison is a cross-language dependency, not an off-the-shelf capability; **empirical** shadow via `observe` mode is what v0.1 gets. Recorded as ADR-0013 amendment 3. |
| OQ-034 | Is there agent-level revocation in AgentCore — disabling a workload identity outright — rather than only deleting its credential providers? | AWS AgentCore teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | The SDK exposes `delete_*_credential_provider` (revoking what an agent can *reach*) but no workload-disable call. If the control plane has one it is the first true `D9` answer in twelve projects; if not, revocation-by-credential is the best available pattern and we should build agent-level revocation ourselves. |
| OQ-035 | Do AgentCore memory namespaces enforce authorization, or is `/actor/Jane/` readable by any caller who names it? | AWS AgentCore teardown | | Phase 5 | **partly resolved** | Server-side authorization is not inspectable, but the SDK narrows the blast radius meaningfully: `build_namespace_params` requires exactly one of `namespace` (exact) or `namespace_path` (prefix) and **refuses wildcards** — `"Wildcards (*) are not supported in namespaces."` (`_utils/namespace.py:18-25`) — so `/actor/*/` cannot sweep every actor's memory. Whether a *named* namespace is authorized against the caller remains unknown. Design conclusion regardless: **pair enforced scopes (ADK's composite key) with namespace hierarchy**, because a path is a convention and a primary key is not. |
| OQ-036 | **Class-level unknown:** 52 of 173 probes are `unknown` for AWS AgentCore because it is a client SDK over managed services — concentrated in `K` sandbox (8), `L` orchestration (7), `B` runtime (6), `A`/`C` (5 each), `P` storage (4). | AWS AgentCore teardown | | Phase 5 | closed 2026-08-30 — accepted as a class-level unknown, not absent (already marked accepted) | **Deliberately `unknown`, not `absent`.** The distinction is the methodological spine of this study: I read the SDK surface, not the service implementation, so absence of an API is not absence of a capability. Resolving these would require an AWS account with AgentCore access plus service documentation, which is out of scope. **This is the only project where the evidence class degrades at subsystem scale**, and it is recorded here rather than silently coded as `absent` — which would have overstated nine gaps in the capability matrix. |
| OQ-037 | Is there an escape hatch for a deliberately compatible workflow change in MAF — a way to declare "this edit does not invalidate checkpoints"? A bytecode hash has no notion of a semantically-null edit. | Microsoft Agent Framework teardown | | Phase 5 | closed 2026-08-30 — decided in ADR-0011 amendment 3: brittle in the safe direction, no escape hatch built | I searched and found none. **Design conclusion regardless** (ADR-0011 amendment 3): accept the brittleness for v0.1, because a false *incompatibility* costs a restart while a false *compatibility* costs silent corruption of the kind LangGraph exhibits (verified: renamed node → resume returns `[]`, no error, work lost). If an escape hatch becomes necessary it must be an explicit operator assertion recorded on the run, not a loosening of the default. |
| OQ-038 | Does MAF's telemetry follow GenAI semantic conventions with a declared vendor namespace, as ADR-0010 now requires? | Microsoft Agent Framework teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Not audited — this was a targeted pass and `_telemetry.py` was out of scope. Recorded `unknown` rather than assumed. Low stakes for our design: ADR-0010's requirements are already settled from Cloudflare, Pydantic AI and ADK. |
| OQ-039 | How deep is MAF's `hyperlight` integration — is a micro-VM the default execution boundary for tool code, or an opt-in package? | Microsoft Agent Framework teardown | | Phase 5 | closed 2026-08-30 — a question about another project's system; no Phoenix decision waits on it | Matters for ADR-0009's process-boundary half: if micro-VM startup is cheap enough to be the default, that is a stronger isolation baseline than the container/process boundaries used elsewhere in the study. |
| OQ-040 | Is Agent Control's `namespace_key` enforced in the schema, or only a filter parameter? | Agent Control teardown | | Phase 5 | **resolved** | **Enforced, and more strongly than anywhere else in the study.** `namespace_key` is in the composite **primary** key *and* in composite **foreign** keys on the association tables, with the intent stated in a comment: "Composite FKs enforce same-namespace references on both sides" (`server/src/agent_control_server/models.py:41-80`). Omnigent and ADK prevent unscoped *reads*; this also prevents cross-namespace *writes*, so binding a policy in namespace A to a control in namespace B is a constraint violation. For a policy system that is the more dangerous case. **I had recorded it as a filter field after reading only the API models** — third time in this study that the schema overturned an inference from an API surface. |
| OQ-041 | Does Agent Control's `observe` mode support running a candidate control against live traffic alongside an enforced one (true A/B shadow), or only non-blocking evaluation? | Agent Control teardown | | Phase 5 | **resolved** | **True A/B shadow.** All controls bound to a target evaluate **concurrently** under a semaphore, each recording its own `ControlExecutionEvent`, and only `deny`/`steer` affect the outcome (`engine/src/agent_control_engine/core.py:662-780`). So a candidate control runs on the same live traffic as the enforced ones without changing behaviour. **One caveat carried into ADR-0013 amendment 2**: a `deny_found` event cancels remaining evaluations, so observe controls may record no verdict for *denied* traffic — biasing the shadow sample away from exactly the cases a candidate policy most needs comparing on. Our observe evaluations must be exempt from deny-triggered cancellation, accepting the latency cost. |

---

## Deferred with intent

Things consciously not decided in v0.1, recorded so they are not mistaken for
oversights.
| OQ-042 | Read the SubstrATE source (`github.com/agent-substrate/substrate`): actor eviction, suspension expiry, and whether a suspended actor's identity is stable across resume onto a different worker. | ADR-0016 / SaaS repositioning | ADR-0016 | Phase 7 | open — parked (2026-08-27: v0.1 provider is gVisor checkpoint-and-kill; revisit only if a provider change is proposed) | Directly gates ADR-0016 moving to Accepted, and gates spike 05's second gate (tool interception across a sandbox move). Probe: clone and read; do not infer from the AX teardown, which only observes the client side. |
| OQ-043 | Can **Pydantic AI**'s native tool execution be disabled so every tool call crosses the `ToolCall → ledger → ToolResult` boundary? Tools are registered Python functions — the decorator's implementation is `agent/__init__.py:2423 @ b48ee38`, usage documented at `:2460`; `:2399` was cited in error (an `@overload` stub) and the citation was corrected 2026-08-27 — so the boundary should be a **decorator**; that is the claim spike 06 must confirm. | SaaS repositioning; harness decision 2026-08-27 | ADR-0004, ADR-0014 | **resolved 2026-08-27 — SPIKE 06, PASS** | **Answered, and the question was framed wrongly.** The boundary is a **toolset**, not a decorator: `ExternalToolset.call_tool` raises unconditionally (`toolsets/external.py:46 @ b48ee38` *(citation corrected 2026-08-28: the unconditional raise is at `:46`; `:44` is the `call_tool` parameter line)*) while the tool is still advertised to the model (`:36`), so the SDK is **never given an executable body** — nothing to disable, no wrapper to route around. `DeferredToolRequests` ends the run (`_deferred.py:27`) and `deferred_tool_results=` supplies platform results on resume (`agent/__init__.py:1189` *(citation corrected 2026-08-28: `:1139` is inside the FIRST `@overload` stub of `Agent.iter` — `@overload` at `:1132`, `def iter(` at `:1133`, body `...`; a second overload runs `:1157-1164`; the implementation is `@asynccontextmanager async def iter(  # noqa: C901` at `:1183`, whose `deferred_tool_results: DeferredToolResults | None = None,` parameter is at `:1189`. This is the same `@overload`-stub error this row already records correcting for `:2399`, repeated seven citations later.)*). **57 gate assertions** *(18 → 26 → 47 → 53 → 57; corrected 2026-08-28 from `spikes/06-tool-interception/RESULT.md`, which `make check` now sums — the count was never re-read after rounds 2 and 3 added gates, round 4 added six more, and round 5 added four: the intent/dispatch identity check, the duplicate-registration refusal, one key spanning two runs, and the SDK-auto-injected wrapper)*, oracle = a filesystem side channel the harness never touches. The `FunctionToolset` control shows `@agent.tool` **is** the bypass, so Phoenix never uses it. Pin **fixed** at `pydantic-ai-slim == 2.35.0`, established by **twenty-two** byte-identical file digests because `uv-dynamic-versioning` + no git tags means the tree cannot state its own version *(amended 2026-08-27, superseding "five": the five-file pin left `native_tools/__init__.py` — the module that decides which vendor-hosted tools may be admitted — unpinned, so review's own control worked again on exactly the module that governs admission)* *(amended again 2026-08-28, superseding "sixteen": that pin omitted `models/__init__.py`, where admission is ACTUALLY decided — `resolve_request_tools` filters native tools against `supported_native_tools` — so the filter could be replaced in a scratch SDK copy with `pin holds: 16 files` and all 47 gates still green; `profiles/__init__.py` and `capabilities/__init__.py` joined it)* *(amended a third time 2026-08-28, superseding "nineteen": that pin omitted `tool_manager.py`, where the tool body is ACTUALLY invoked — `await self.toolset.call_tool(...)` at `:1008` inside `_raw_execute` — so a scratch-copy `except NotImplementedError:` there turned `ExternalToolset`'s fail-closed refusal fail-OPEN with `pin holds: 19 files` and all 53 gates green; `capabilities/_tool_search.py` and `toolsets/_tool_search.py` joined it because `Agent.__init__` auto-injects `ToolSearch`, which always wraps the boundary toolset)*. **Amended 2026-08-27 (round 3):** the toolset argument is necessary and was not sufficient, because the harness returned a raw `Agent`; `agent.override(native_tools=[WebSearchTool()])` against `FunctionModel` (which supports native tools, `models/function.py:242-244`) delivered a vendor-hosted tool to the model with no Phoenix involvement. The harness now exposes no public name that is, or returns, an `Agent`, and the effect ledger was rewritten to `spec/01-schema.md:332`'s statuses and `spec/02-consistency.md`'s phase predicates (it had used a status `settled` that the schema does not define, and no run-lease predicate at all). Unresolved and now urgent: `sdk_in_process` "collapses to a function call" (superseded 2026-08-28 by OQ-056: sidecar over a Unix socket) cannot hold across a **Go** control plane and a **Python** harness — see OQ-056. Vendor-hosted tools stay unsupported: `ExternalToolset` cannot express them, as there is no local body to withhold. | `spikes/06-tool-interception/RESULT.md` | **Rescoped 2026-08-27 to ONE SDK** (was three: Claude Agent SDK, LangGraph, OpenAI Agents SDK). If native execution cannot be intercepted for a tool, that tool **cannot be registered in v0.1** — `spec/08` row 30c, marked NOT VERIFIED. Vendor-hosted tools (web search, code execution, file search, computer use) execute on the vendor's side; an `observed` ledger status was specified and **retracted** on 2026-08-27 because it promised reporting that cannot hold when the connection drops. Any future admission must be *provider-reported use*, require a durable provider receipt for anything stronger, and carry egress and confidentiality constraints. **Spike 06 scope:** (1) register zero tools and assert no ledger-bypassing path; (2) wrap `@agent.tool` and assert every call produces an `effect_ledger` row *(superseded 2026-08-28: item (2) was never executed as written — wrapping `@agent.tool` is the bypass, not the boundary. The spike declares tools through `ExternalToolset` and additionally exposes no `Agent`; see the answer column.)*; (3) fix the pinned version, which is a placeholder at 2.35.0 until then *(superseded 2026-08-27: the version is no longer a placeholder — it is fixed by SHA-256 file digests (sixteen then; nineteen after round 4; **twenty-two** since round 5, both 2026-08-28) in `spikes/06-tool-interception/pinned_digests.json`, asserted before collection by `conftest.py`)*. |
| OQ-044 | Does an enterprise buyer accept that quorum/separation-of-duties approval is unsupported, or is it a gating requirement for the first deployment? | Multi-party approval refusal | ADR-0015 | Phase 7 | decided 2026-08-30 — bind an external approval system as the approver: decided_by may name a service principal carrying an external ticket reference; no quorum engine. spec/09 section 7, ADR-0015 amendment 4, spec/06 amended | Determines whether the focused approval teardown happens before or after first deployment. The refusal is defensible on evidence (one precedent, one `decided_by`), but it is a **narrowed enterprise claim** and must be said out loud in a sales conversation, not discovered in one. |
| OQ-045 | Spike 03's own module docstring still says "the eight scenarios review listed as unverified" (`spikes/03-postgres/test_postgres.py:2`) while the file runs nine (`s1_epoch_append_vs_rewind` … `s9_approver_must_be_human`, `:77-605`) and prints "PASS — all nine scenarios hold against real Postgres" (`:708`). Should the docstring be corrected, or does it read defensibly as history of what review had listed? | Round-2 review, out-of-scope residue | Satiya | 2026-09-15 | resolved 2026-08-27 — docstring no longer hand-counts | The user-facing half of the same stale count was fixed this round (`tools/validate_spec.py`, skip message: eight → nine). Recorded rather than fixed because the docstring is a sentence about what a past review said, and this repo's rule is to amend with a date rather than silently renumber history. |
| OQ-046 | `make check` is not reliably reproducible from the documented quick procedure. (a) The procedure (docker run, wait 5s, `make check`) omits applying `spikes/03-postgres/schema.sql`; without it the gate fails with sqlstate 42P01 undefined_table, and `_apply_spec_ddl()` deliberately rolls back so it never seeds the spike's tables. (b) `make check` runs `tools/test_validate_spec.py` first, whose scratch-copy runs invoke the same validator against the same default DSN, leaving live backends on that database; a subsequent real gate run then failed on `run_events_tenant_id_run_id_fkey`. Should the schema apply be part of the documented procedure, part of the gate, or neither? | Round-2 review, out-of-scope residue | Satiya | 2026-09-15 | open — at build (2026-08-30) — one fix: per-invocation database name; the documented procedure applies the schema | Observed on an untouched tree from a fresh container: exit 2, exit 0, exit 0, then a run that hung past ten minutes. Run directly, the spike passes twice in a row (42/42), so the spike itself is re-runnable; it is the shared server under the self-test that breaks it. This round only improved the failure MESSAGE (`postgres gate failed: <tail> (did you apply schema.sql?)`); the reproducibility question is untouched. |
| OQ-047 | The Postgres gate has no isolation. The DSN is a single hard-coded database and `fresh()` (`spikes/03-postgres/test_postgres.py:44-56`) wipes a shared fixture (`DELETE FROM ... WHERE tenant_id=1`) that every scenario reuses, so two `make check` runs on one machine corrupt each other. Should the gate create a per-run database or schema, or is a documented "one gate run at a time" rule enough? | Round-2 review, out-of-scope residue | Satiya | 2026-09-15 | open — at build (2026-08-30) — one fix: per-invocation database name; the documented procedure applies the schema | Measured while a second session shared the container: 3 of 6 fully serial runs failed with three distinct signatures (`TypeError: 'NoneType' object is not subscriptable`, `ForeignKeyViolation ... run_events_tenant_id_run_id_fkey`, `UndefinedTable: relation "effect_ledger" does not exist`). On a private container the same unmodified binary passed 5 of 5. A gate that goes red for reasons unrelated to the artefact teaches re-running until green, which is what VERIFICATION-RULES rule 6 exists to stop. The first of those three signatures was a real defect and is fixed this round (F2: the `k4` fixture precondition). |
| OQ-048 | `DESIGN.md:352` still asserts "We own a thin internal harness over the vendor SDKs" (plural) while ending with the citation `` (`v01-boundary.md:91`) ``, and `synthesis/v01-boundary.md:91` now reads "on **Pydantic AI 2.35.0** (amended 2026-08-27, superseding \"over the vendor SDKs (Claude Agent SDK, LangGraph, OpenAI Agents SDK)\")". Is the plural superseded residue of the two-SDK plan, or a deliberate statement that the adapter boundary keeps a second SDK possible? | Round-2 review (consistency R3-01, gate N2), closing judge | Satiya | 2026-09-15 | resolved 2026-08-27 — DESIGN.md:352 now says Pydantic AI | Raised by two independent lenses at medium. The contradiction was **created by round 2**: before `73fc21e..b4cef6e` both sentences said "over the vendor SDKs" and agreed; round 2 corrected the boundary row (F1) and re-anchored this citation `:87` → `:91`, so the claim now cites the line that declares its own wording superseded. `DESIGN.md` carries no "the boundary wins" precedence clause, unlike `synthesis/reference-architecture.md:3`, so nothing governs the disagreement. `DESIGN.md:35-36` and `:117` already carry the corrected claim, so the document also contradicts itself. Not fixed by the judge: the residual finding is medium, not blocking, and the closing mandate fixes only blocking one-line residue. |
| OQ-049 | `spec/01-schema.md:126` carries the same plural inside normative DDL — `-- a thin internal harness over the vendor SDKs, so there is no third-party` — as an undated active claim. Should it be amended with a date like `v01-boundary.md:91` and `DESIGN.md:35` were? Answer together with OQ-048. | Round-2 review (consistency R3-02, gate N3) | Satiya | 2026-09-15 | resolved 2026-08-27 — spec/01:126 now says Pydantic AI | Untouched by either round, so outside both rounds' scope. No gate can reach it: `_scanned_files()` does include `spec/01-schema.md` (`tools/validate_spec.py:609`) but `sql` is absent from `_SCANNED_FENCE_LANGS` (`:632`, `{"text", "", "http", "yaml"}`), so the superseded-claims check never sees SQL comments. Whether that fence-language gap is itself worth closing is part of the same decision. The remaining repo instances of the plural are governed and should stand: `synthesis/reference-architecture.md:228` by the frozen "boundary wins" sentence, `decisions/ADR-0004-runtime-is-adapter-based.md:131` by the redo-4 amendment directly below it, and `synthesis/scope-reconciliation.md:224`/`:240` as the dated record of the decision itself. |
| OQ-050 | `tools/superseded-patterns.txt:18,21` state the two-SDK sequencing, superseded 2026-08-27, as live fact in their rationale column: "the first adapter is the Claude Agent SDK, not Claude Code" and "replaced by the Claude SDK adapter, then the OpenAI SDK adapter". Is that column normative prose that must be amended with a date like any other claim, or an operator note exempt by being outside the scan? | Round-2 review (consistency R3-03) | Satiya | 2026-09-15 | resolved 2026-08-27 — patterns rationale now names Pydantic AI | Last touched at 88ee4e4, before both rounds, so outside their scope. The patterns themselves are unaffected — only the human-readable third column is stale — and `tools/` is not in `_scanned_files()`, so the gate cannot police the reasons it prints. `decisions/ADR-0004-runtime-is-adapter-based.md:132` supersedes exactly this sequencing ("**One harness, on Pydantic AI 2.35.0**, replacing \"Claude Agent SDK then OpenAI Agents SDK\""), and OQ-043 records it as rescoped. |
| OQ-051 | The Postgres gate's new failure-cause heuristic (`tools/validate_spec.py:546`) takes the **last** qualifying unindented stderr line, but psycopg prints `DETAIL:` after the exception class, so the class is discarded again on the dirty-database failure `make check` actually hits. Should the heuristic prefer the first line matching `^\w[\w.]*Error\|^psycopg\.errors\.` and fall back to the current rule? | Round-2 review (scope-creep SC-10) | Satiya | 2026-09-15 | open — at build (2026-08-30) — trivial fix to the stderr failure-cause heuristic | Observed at HEAD: `x postgres gate failed: DETAIL:  Key (tenant_id, run_id)=(1, r1) is still referenced from table "run_events". (did you apply spikes/03-postgres/schema.sql? see spikes/03-postgres/RESULT.md:74)`. Round 2's F4-gate fix is a real improvement over round 1's bare `x postgres gate failed` and works on the empty-database case it was tested against (`psycopg.errors.UndefinedTable: relation "approvals" does not exist`); it is the dirty-database case that still loses the class. Not fixed by the judge: nit severity, and changing the heuristic is verifier surface. Related to OQ-046 and OQ-047. |
| OQ-052 | Two counts in `check_counts()` are compared between hand-written numbers rather than against the artifact. (a) `spec_docs` is asserted only between `synthesis/scope.yaml` and `README.md`, so both can move together undetected. (b) `spike_assertions` is derived from each `RESULT.md`'s hand-written `**Gate assertions: N**` headline (`tools/validate_spec.py:764-782`), never from running the suite. Should either be derived from the artifact — and if so, does `contracts/` count toward `spec_docs`, and can the gate afford to run all four spikes to total them? | Round-1 review (consistency F2); closing judge, after (b) drew blood | Satiya | 2026-09-15 | open — at build (2026-08-30) — derive spec_docs from `ls spec/*.md`; keep spike_assertions from RESULT headlines (running four spikes in make check is too slow) | (b) is not hypothetical: round 2's own F2 fix added a 43rd assertion to spike 03 and no number moved, and `make check` stayed green on the false count through two full review rounds. The closing judge measured it directly — `73fc21e` prints `42 passed, 0 failed`, `b4cef6e` prints `43 passed, 0 failed` — and corrected the four declared numbers (RESULT.md 42→43, README 116→117, scope.yaml 116→117) in the closing commit. The gate did not catch it and still would not. (a) was verified by mutation in round 1: moving `spec_docs` to 11 in both files leaves `counts vs source of truth` green. `scope.yaml:83-86`'s comment was corrected in round 2 to stop claiming otherwise. |
| OQ-053 | A document freeze is invisible to every tool. `tools/check_exit_criteria.py:136` reads only the literal `<!-- status: final -->`, so D4 and the deliverables table cannot distinguish a document still being edited from one closed on 2026-08-27; and frozen documents remain inside the superseded-claims scan, so a future scope change could make the gate demand an edit to a document the freeze says must not be edited. Should `frozen` become a parsed status, and should a superseded claim in a frozen file get a dated note rather than a rewrite? | Round-1 implementer and review (F4/SC-07, SC-01), round-2 implementer | Satiya | 2026-09-15 | decided 2026-08-30 — frozen stays prose, not a parsed status; a frozen document stays inside the superseded-claims scan | Both halves were tried and backed out in the two rounds rather than decided. Round 1 added a second `<!-- status: frozen 2026-08-27 -->` marker and a `_FROZEN_FILES` scan exclusion; round 2 removed both — the marker because nothing parsed it, the exclusion because it measurably removed real coverage (with it in place, planted mutations in `synthesis/reference-architecture.md` and `synthesis/exit-criteria.md` were missed; with it gone they are caught, and both files are clean today). What remains is the freeze recorded only in dated prose. Teaching the parser a new status is genuine new surface and belongs in a decision, not a fix. |
| OQ-054 | Should `synthesis/scope.yaml` hold the harness choice (Pydantic AI 2.35.0) as an asserted scope value, and if so against which document — `v01-boundary.md` Tier 1 #4, ADR-0004, or both? | Round-1 implementer and review (SC-03), round-2 implementer | Satiya | 2026-09-15 | decided 2026-08-30 — yes: synthesis/scope.yaml asserts harness: pydantic-ai 2.35.0 against spec/07's stated pin, checked by tools/validate_spec.py's check_scope_source_of_truth(), with a mutation control in tools/test_validate_spec.py | Round 1 added `harness` and `adapters_shipped` keys; round 2 deleted them, because `check_scope_source_of_truth()` reads only `tiers`, `deferred`, `never`, `counts` and `owed_specs` — so the file created to stop unpoliced scope claims was itself carrying two. Verified by mutation in both rounds: rewriting `harness` to `LangGraph 9.9.9` left the check silent. Deletion was the right resolution for the round, but the underlying question is unanswered: the harness is the single scope value this whole change propagated, and no machine assertion covers it anywhere. It is also the value OQ-043 says is a **placeholder until spike 06 fixes the pinned version** — *amended 2026-08-27: that half is now answered and only the scope-assertion half remains open. Spike 06 fixed the version by **digest**, not by declaration: twenty-two SHA-256 digests *(amended 2026-08-28, superseding "nineteen", which superseded "sixteen": round 5 added `tool_manager.py`, where the tool body is actually invoked, plus `capabilities/_tool_search.py` and `toolsets/_tool_search.py`; `models/__init__.py`, `profiles/__init__.py` and `capabilities/__init__.py` were added after review showed the admission filter could be neutered under the sixteen-file pin)* of the installed `pydantic_ai` files the gates depend on (`spikes/06-tool-interception/pinned_digests.json`), checked before collection. `pydantic-ai-slim == 2.35.0` is a label for those digests, because `uv-dynamic-versioning` + no git tags means the source tree cannot state its own version. The open question is unchanged: no check in `tools/` asserts the harness choice against any document.* |
| OQ-055 | Shared data plane with strong isolation (fleet-wide bin-packing) or per-customer VPC data plane (ref-arch D3; packing only within a customer)? Under the v0.1 provider (gVisor checkpoint-and-kill, zero idle cost) the saving no longer depends on cross-customer packing, so per-customer is the honest default. | Sandbox provider discussion 2026-08-27 | ADR-0016 | when a provider change is proposed | open | Decide only if a provider change is proposed; not a v0.1 blocker. Owner: Satiya. |

| OQ-056 | **`sdk_in_process` (superseded name, renamed 2026-08-29) cannot mean "a function call" across a Go control plane and a Python harness.** `spec/07` said the in-process transport "collapses to a function call" (superseded 2026-08-28 by this decision) and `integration_mode` was CHECK-restricted to `sdk_in_process` (superseded 2026-08-29); the control plane is **Go** (ADR/scope) and the harness is **Pydantic AI**, i.e. Python. Spike 06 ran Python end to end, so it proved the tool boundary but says nothing about the process boundary. Which is it: a Python control-plane component, a Go plane driving a Python sidecar over the §07 frames (making the "deferred remote transport" the *shipped* one), or Go embedding CPython? | **SPIKE 06** (surfaced, not resolved) | ADR-0004, spec/07, spec/01 `integration_mode` | **Tier 1, before item 4** | resolved 2026-08-28; amended 2026-08-29 (spike 05 T2) — one container, the Go driver spawns the Python harness and passes it an inherited socketpair; the sidecar-over-a-Unix-socket answer is superseded. See DESIGN §7a, spec/07 §Transport, ADR-0016:63 | The three answers imply different failure modes, different deployment units and a different meaning for `integration_mode`. This is a genuine contradiction between two accepted decisions, not an unanswered design question, so per the 2026-08-27 classification rule it must be **corrected** — but the correction is a design choice, so it is filed as an OQ with an owner rather than patched silently. | — |
| OQ-057 | **The default dispatch route supplies its own evidence for the run-lease predicate.** `harness.py._lease_for` MINTS the `RunLease` that `claim()` is then checked against, and `dispatch` sets `owner = lease.holder` and `fence = lease.fence_token` from that same object — so on the default route `spec/02-consistency.md:246-248` predicate (b) holds by construction and can never fail. Every negative control that shows it refusing (no lease, expired, foreign holder, another run, superseded fence) injects `lease=` explicitly, so the predicate is verified only for a caller that volunteers a bad lease. What would make it load-bearing is a lease **read** from a store the claimant does not write, which is spike 03's territory. | Round-4 verifier (sdk-boundary lens), 2026-08-28 | Satiya | 2026-09-15 | closed 2026-08-30 — spike-local simplification, recorded in RESULT.md; the real harness is spec 12/13 | Recorded as simplification 12 in `spikes/06-tool-interception/RESULT.md`, not fixed in round 4: fixing it means giving the harness a lease store, which is new scope. |
| OQ-058 | **`make check` runs the Postgres gate twice in one invocation, and the second run inherits the first's residue.** `tools/test_validate_spec.py` (Makefile:34) exercises the gate as a negative control and leaves a `runs` row behind; `tools/validate_spec.py` (Makefile:35) then runs it again against the same fixed DSN. Observed failures on a database carrying that state: `ForeignKeyViolation ... Key (tenant_id, run_id)=(1, r1) is still referenced from table "run_events"`, and separately `FAIL NC fixture: the k4 ledger row exists before the claim` + `status stays 'denied', never 'indeterminate'` + `an APPROVED effect can be claimed`. A freshly-schema'd database exits 0, so this is state-dependent, not a deterministic HEAD failure — but a gate whose verdict depends on residue from an earlier run in the same `make check` is the non-idempotence shape `VERIFICATION-RULES.md` already records for `schema.sql`. | Round-4 verifier (sdk-boundary + ledger-spec lenses), 2026-08-28 | Satiya | 2026-09-15 | open — at build (2026-08-30) — one fix: per-invocation database name; the documented procedure applies the schema | Out of spike 06's lens: `spikes/06-tool-interception` is green standalone and inside `make check`. The fix is ownership of the Postgres fixture, in `tools/`. |
| OQ-059 | **`check_postgres_gate()` shares one fixed-name, mutually-destructive fixture across every nested run.** `spikes/03-postgres/test_postgres.py:44-56` `fresh()` DELETEs every tenant-1 row and re-creates run `r1`; `tools/test_validate_spec.py:58,189,207` runs a full `validate_spec.py` inside each repo copy with no `PHOENIX_PG_DSN` override, so every control re-runs that same gate against the same database, and leaked temp-copy `validate_spec.py` processes were observed still connected to `127.0.0.1:55433/spike` after their run. That is the ownership half of rule 7 and it makes the repository gate nondeterministic. Strengthens OQ-058. | Round-4 verifier (ledger-spec lens, out of lens), 2026-08-28 | Satiya | 2026-09-15 | open — at build (2026-08-30) — one fix: per-invocation database name; the documented procedure applies the schema | Proposed fix direction: a per-run DSN or a uniquely-named database per nested invocation, so no two runs share a fixture. |
| OQ-060 | **`check_counts()` cannot see three documents that state spike-06 counts.** It compares `spike_assertions` only between each `RESULT.md` headline, `README.md` and `synthesis/scope.yaml` (`tools/validate_spec.py:764-782,903-904`). `spec/07-adapter-protocol.md:326`, `synthesis/v01-boundary.md:39`, `decisions/ADR-0016...:101` and — *added 2026-08-28 (round 5)* — **`Makefile:50`**, the comment that exists to explain why spike 06 is in `make check`, each state a spike-06 assertion count that **no check reads**, and all were stale for three rounds (18 / 26 / 18 against an actual 47). Round 4 corrected the first three by hand and missed `Makefile:50`, which still read "47 as of 2026-08-27" at an actual 53; round 5 corrected all four to 57 — the fifth hand-correction of the same class. Strengthens OQ-052(b). | Round-4 verifier (hygiene lens, out of lens), 2026-08-28 | Satiya | 2026-09-15 | open — at build (2026-08-30) — scan every tracked .md for a spike-06 assertion count; generate the mutation table from mutate.py | Proposed fix direction: have `check_counts()` scan every tracked `.md` for a spike-06 assertion count and compare each against `RESULT.md`, rather than enumerating three known sites. |
| OQ-061 | **Two counts inside `spikes/06-tool-interception/RESULT.md` are still hand-kept prose.** The mutation table's per-mutation "gates broken" figures and the pin's file count are regenerated by hand from `mutate.py` / `verify_pin.py` output. `mutate.py` GENERATES the table (round 3's fix) but nothing asserts the pasted copy matches a fresh run, which is exactly how the round-2 table went stale (3 claimed, 6 measured). | Round-4 implementer, 2026-08-28 | Satiya | 2026-09-15 | open — at build (2026-08-30) — scan every tracked .md for a spike-06 assertion count; generate the mutation table from mutate.py | Proposed fix direction: `make spike06-mutations` writes the table to a file and a gate diffs it against `RESULT.md`, or the numbers move out of prose entirely. |
| OQ-062 | **The identity check `dispatch()` now performs is a read-then-act pair, where the deployed form is one predicate.** `PhoenixHarness.dispatch` reads the intent row (`EffectLedger.row_for`) and compares `run_id`, `tool_name` and `args_json` BEFORE calling `claim()`. In Postgres those comparisons belong inside the claim `UPDATE`'s `WHERE` (`spec/02-consistency.md:227-248`), alongside predicates (a) and (b), so the row cannot change between the read and the claim. In-process and single-threaded nothing can interleave, so the spike cannot show the difference — the same shape simplification 4 already records for approval and simplification 5 for the run lease. Should `claim()` take the expected `kind` and `request_digest` as arguments so the comparison is part of the one statement? | Round-5 implementer, 2026-08-28 | Satiya | 2026-09-15 | closed 2026-08-30 — spike-local simplification, recorded in RESULT.md; the real harness is spec 12/13 | Recorded rather than fixed: moving the comparison into `claim()` changes the ledger's public signature, which is spike 03's territory (real Postgres, real concurrency) and new scope here. The predicate itself is verified — `test_gate3_dispatch_of_a_call_the_row_does_not_describe_is_refused` and two mutations — its ATOMICITY is not. |
| OQ-063 | **Nothing forces a `_coverage_frontier` module into the pin when an assertion starts depending on it.** `verify_pin.frontier()` records the one-level edge and `check()` compares it, so a NEW import fails the gate — but a module already on the frontier that an existing assertion depends on stays there silently. That is now the third instance in three rounds (`native_tools/__init__.py`, `models/__init__.py`, then `tool_manager.py` + the two `_tool_search.py` modules), and each time the frontier's own justification — "none of which any assertion here touches" — was the sentence that was false. Should the frontier carry a per-module reason rather than one blanket clause, or should the pin follow the actual runtime call path (e.g. recorded from a traced run) instead of the import graph? | Round-5 implementer, 2026-08-28 | Satiya | 2026-09-15 | resolved 2026-08-28 — by decision: SDK pinning is a release-pipeline lockfile concern; the spike digest file is documentation and will not be extended | The import graph is the wrong oracle for "what decides this behaviour": `_tool_execution.py` imports `tool_manager` and the frontier saw the edge, but nothing connected the edge to the assertion that depended on it. A traced-call-path pin is genuine new surface and belongs in a decision, not a fix. |
| OQ-064 | **The pin stops one delegation hop short of the chain the agent actually runs.** `verify_pin.REQUIRED_PINS` holds the site where a tool body is invoked (`tool_manager.py:1008`) and the leaf that refuses it (`toolsets/external.py:46`), but not the hops between: the measured chain is `ToolSearchToolset -> PreparedToolset -> CombinedToolset -> _AgentFunctionToolset`, whose `WrapperToolset.call_tool` (`toolsets/wrapper.py:63`), `CombinedToolset.call_tool` (`toolsets/combined.py:94`) and `PreparedToolset.get_tools` (`toolsets/prepared.py:23`) are all on `_coverage_frontier`, unpinned. In a scratch SDK copy, `except NotImplementedError: return 'SILENTLY FABRICATED BY THE SDK'` at either `call_tool` left `pin holds: 22 files` and 57 gates green. Should these three be pinned, or is this the case OQ-063 exists to settle? | Round-6 verifier (sdk-boundary lens), 2026-08-28 | Satiya | 2026-09-15 | resolved 2026-08-28 — same decision as OQ-063 | Filed rather than fixed: the fix is three `REQUIRED_PINS` entries plus a pin regeneration and a 22 -> 25 count propagation across eight documents, which is not the one-line residue the closing mandate may apply. **Why not blocking.** The judge measured the harness's full public path (`run` -> `resolve`, body executed, row settled `succeeded`) with `call_tool` instrumented on all three classes: **zero** invocations. External calls are deferred at `_tool_execution.py:921-928` and resolved from supplied results, so `ExternalToolset.call_tool` is never reached through this harness and neither are the hops above it. The demonstrated fail-open is therefore on bytes the harness does not execute, and every assertion that *reads* those modules is fail-closed: gate 13c's `flatten()` walks `.wrapped` / `.toolsets` **attributes** set in `__init__` (a rename goes red), `externals[0].tool_defs is h._defs` is an identity check on the harness's own object, and `info.function_tools == ['send_email']` goes red if `prepared.py` adds or drops a tool. What SB6 correctly shows is that `_coverage_frontier_why`'s blanket justification overreaches for a fifth round running — which is precisely OQ-063's subject. Answer this row and OQ-063 together; pinning one more hop per round is the treadmill OQ-063 exists to end. |
| OQ-065 | **The policy boundary fails OPEN, and phase 2 of the five-phase lifecycle is unreachable.** `PhoenixHarness.dispatch` reads the verdict and acts on one equality — `if verdict == "deny":` (`harness.py:669`) — with no closed verdict set; the constructor default is `lambda name, args: "allow"`. Measured 2026-08-28 at `36a2b22` through the public surface (`intend` then `dispatch`): `'deny'` gives `('denied','policy_denied')` with the body not run, but `'require_approval'`, `'denied'`, `'DENY'` and `None` **all** run the body (`log=['SENT:a']`) and settle `('succeeded', None)`. `'require_approval'` — the one verdict phase 2 exists for — is among them. `EffectLedger.await_approval` (`harness.py:259`) has exactly one caller in the spike (`test_gate.py:1171`, driving the ledger directly), so no `PhoenixHarness` route ever produces an `awaiting_approval` row and the harness runs phases 1-3-4-5. Should the verdict set be closed (unrecognised -> `abandoned`), and should the harness gain an approval route at all? | Round-6 verifiers (ledger-spec + sdk-boundary lenses), 2026-08-28 | Satiya | 2026-09-15 | resolved 2026-08-28 — fixed in harness.py: verdict enum, require_approval parks the row, unknown verdict raises; gates 7b/7c | `spec/02-consistency.md:188-194` lists five phases; `:190` is `2. APPROVAL … -> awaiting_approval` and `:189` puts policy evaluation in phase 1. This is the fail-open shape the same file refuses elsewhere and says so out loud (`_reject_sdk_surface` refuses even `native_tools=None`; `RunLease.authorises` treats `fence is None` as failure). Nothing can go red on it: gate 7 passes the literal `"deny"` and no other verdict, and none of the 19 mutations touches the comparison. Not fixed by the judge: closing the verdict set adds a refusal, a gate and a mutation — new surface under this round's constraint — and whether the harness should have an APPROVAL phase is a design choice, not a fix. |
| OQ-066 | **A tool result that `json.dumps` cannot encode strands its ledger row at `claimed` forever, after the effect has already happened.** `dispatch()` computes the serialisation *inside* the settle call (`harness.py:727-729`, `self.ledger.settle(..., json.dumps(result, sort_keys=True), ...)`), so the `TypeError` is raised while evaluating the argument and `settle` is never entered. Measured 2026-08-28 at `36a2b22` through `register` / `run` / `resolve`, with a body that appends to a side list and returns `object()`: `TypeError: Object of type object is not JSON serializable`, side effects `['EMAIL:a']`, rows `[('pyd_ai_tool_call_id__send_email','send_email','claimed',None)]` — no `succeeded`, no `indeterminate`, no terminal status. Should the payload be encoded before settling, so the honest verdict is `indeterminate`? | Round-6 verifier (sdk-boundary lens, out of lens), 2026-08-28 | Satiya | 2026-09-15 | resolved 2026-08-28 — serialisation failure after dispatch settles indeterminate (result_unserialisable); gate 11b | Contradicts `dispatch()`'s own stated rule at `harness.py:626-633` — "a raise BEFORE the body is entered -> `failed` … a raise AFTER the body is entered -> `indeterminate`" — and reproduces one phase later the stuck-effect state `EffectLedger.append`'s docstring already calls out at `:232-235`. Simplification 7 records that there is no sweeper, so nothing ever reclaims the row. `spec/02-consistency.md:327-328` ("we took a lease, dispatched, and lost contact") supports `indeterminate` here. Proposed direction: encode into a local before `settle`, settling `indeterminate` / `result_unencodable` on failure, with a gate and a paired mutation. Not fixed by the judge: it is a behaviour change plus a new error code, i.e. new surface. |
| OQ-067 | **Simplification 6 labels a spec-sanctioned transition a divergence, hiding a real gap in `spec/02`.** `spikes/06-tool-interception/RESULT.md` row 6 calls `intended -> denied` on a policy verdict "a deliberate divergence". It is not: `spec/01-schema.md:497` draws `+-- policy: deny --> denied` from `intended` as a first-class transition (with `:501` `+-- policy: allow ----------> claimed`), so `EffectLedger.deny()` is conformant. The gap is on the spec side — `spec/02-consistency.md:307-323` gives SQL for `awaiting_approval -> denied` (`:311`), approval-expiry (`:315`) and `-> abandoned` (`:321`), and **no** statement for the policy-deny route that `spec/02:189` itself places in phase 1. Should `spec/02` gain that statement, and should the Simplifications row be reclassified? | Round-6 verifier (ledger-spec lens), 2026-08-28 | Satiya | 2026-09-15 | closed 2026-08-30 — the integrator added the intended -> denied UPDATE to spec/02 | The two normative documents disagree about how a policy denial is written, and the spike's Simplifications table records the disagreement as its own deviation, which sends the next reader to the wrong document. Proposed missing statement: `UPDATE effect_ledger SET status='denied', error_code=$code WHERE tenant_id=$tenant AND idempotency_key=$key AND status='intended';` — also a never-claimed row, so it cannot produce `indeterminate`. Not fixed by the judge: amending normative SQL in `spec/02` is spec surface, not spike residue. Answer alongside OQ-065, which is the same phase. |
| OQ-068 | **`PhoenixHarness.clock` is a constructor argument the class accepts and never reads.** `self.clock = clock` (`harness.py:478`) was the run-lease mint site until round 5 moved the only read to `self.ledger.clock()` (`harness.py:614`) so one clock decides both halves of `spec/02-consistency.md:249`. The remaining `self.clock()` reads in the file all belong to `EffectLedger` (`:320`, `:363`, assigned `:185`). A caller passing `PhoenixHarness(clock=…)` now controls nothing, silently — measured: a harness pinned to `lambda: 1000.0` against a ledger on real `time.monotonic()` dispatches to `succeeded`. The attribute survives only as the target of `mutate.py:216-219` ("mint the run lease on the harness clock"), i.e. the parameter exists so the bug can be reintroduced. Should it be documented as a mutation-only hook, or removed with the mutation rewritten? | Round-6 verifier (ledger-spec lens), 2026-08-28 | Satiya | 2026-09-15 | closed 2026-08-30 — spike-local simplification, recorded in RESULT.md; the real harness is spec 12/13 | Nothing is wrong today; it is undisclosed surface for the next reader, of the class each of the previous rounds produced. `test_gate.py:1132` still passes `clock=clock` and `:1153` `clock=lambda: 1000.0`, and the dated comment at `:1144-1151` still explains a two-clock arrangement that no longer exists in the un-mutated harness. Not fixed by the judge: a one-line comment would be within reach, but the finding is a nit and the honest resolution (document vs. remove) is a choice. |
| OQ-069 | **`model=` is the one tool-bearing SDK surface `_reject_sdk_surface` does not name.** The harness refuses `native_tools=`, `toolsets=`, `tools=` and `capabilities=` by name — whatever their value, so that even `native_tools=None` is a refusal rather than a near miss (`harness.py:429-436`) — on the stated ground that a caller-supplied one "would reach the model without a ledger row". But `model: Any = None` (`harness.py:469`) is accepted unvalidated and handed straight to `Agent(self.__model, …)` (`harness.py:555`), and a provider `Model` carries request passthrough that reaches the wire outside `ModelRequestParameters` entirely (`model_settings.get('extra_body')`, `models/openai.py:1172`, `:2230`, `:2755`). Gate 13d asserts `model_request_parameters.native_tools == ()` across run, resume and stream, but only against `FunctionModel` and `TestModel` — neither of which has a wire — so it covers the SDK's native-tool plumbing and says nothing about a caller-constructed provider model. Should this be documented as a Simplification, or does the invariant need to name the trust placed in `model=`? | Round-6 verifier (sdk-boundary lens), 2026-08-28 | Satiya | 2026-09-15 | closed 2026-08-30 — spike-local simplification, recorded in RESULT.md; the real harness is spec 12/13 | Same class as Simplification 13: the boundary is enforced against accident and against the SDK's own named surfaces, not against a caller who supplies the model. `RESULT.md`'s invariant already scopes itself to a "cooperating in-process caller" and names `h._PhoenixHarness__build()` as its one stated limitation; `model=` is named nowhere, and the Simplifications table has no row for it. Not fixed by the judge: the verifier's own recommendation is documentation-only and explicitly warns that *validating* `model=` would be new surface, so the choice between the two belongs to the owner. |
| OQ-070 | gVisor (release-20260817.0, systrap) does not propagate a Unix-socket inode across containers on a shared `emptyDir` — regular files propagate, the socket never appears; identical pod under runc connects (spike 05 T2, `9f21e88`, bisected against start races, overlay and mount coherence). Is this a known upstream issue, and does a later release fix it? | Spike 05 T2 | ADR-0016, ADR-0009 | when a gVisor release is next bumped | open | Not blocking: the design moved to one container with an inherited socketpair, which is stronger regardless. Probe: search gVisor issues for cross-container UDS on shared mounts; if fixed upstream, two containers become *possible* again but the socketpair stays the default. Owner: Satiya. |
| OQ-071 | Who is the **worker** of `spec/05`'s actor column under `sdk_subprocess`, and does the run container get a database path? `spec/13` §1.1 now says the **control plane** holds the `run_leases` row and that §6's outcomes are reports the plane turns into worker writes, matching `spec/15` §6 ("the run container holds no route to the control plane's database") and `spec/14` §The driver holds only `run_token`. What is still owed is the gate: an assertion that a database credential injected into the run container does not reach the plane. | Judge, cross-spec review of specs 10-19 | Satiya | 2026-09-30 | closed 2026-08-30 — decided in the spec/13 rewrite: the run container gets no database path | Design decided 2026-08-30 in favour of no database path — `spec/13` §1.1, §4, §6 and §7 were rewritten. Decide before spike 05's gates are written; the answer changes the blast radius of a compromised run container. |
| OQ-072 | Is `Start.turn_ordinal_seed` ever needed in a checkpoint-relative form? `spec/12` §5 and `spec/07`'s field comment now define it over the log alone — live `cost.usage.recorded` events for the run, rewind-superseded ones excluded — because the checkpoint payload is opaque bytes no component may parse (`07`:394-400). If a resume ever needs "at or before the last turn in the checkpoint", some component must parse that payload and the boundary `07` §`config` is opaque exists to close reopens. | Judge, cross-spec review | Satiya | 2026-09-30 | closed 2026-08-30 — settled: turn_ordinal_seed is defined over the log alone (spec/12 section 5, spec/07) | Settled for v0.1 as the log-only form. Owner of the counter-example would be spec/13 or spec/18, since `cost.usage.recorded` carries the `step_id` the predicate reads. |
| OQ-073 | Should tenant- and agent-layer knowledge be reachable through the symbolic closure, or only through the always-resident set? `spec/12` §8 now draws context from the **compiled knowledge package**, not the pinned bundle, so a tenant-layer file and an overridden bundle path are decidable. What is undecided is whether a higher-layer file may itself declare edges that extend the closure, or whether it can only ever be `system/**`. | Judge, cross-spec review; spec/16 review | Satiya | 2026-09-30 | closed 2026-08-30 — settled: the knowledge universe is the compiled package, not the pinned bundle (spec/12 section 8) | Universe decided 2026-08-30 (the package). Spans specs 12 and 16. |
| OQ-074 | May a connector dedupe against a customer's own idempotency key? Decided 2026-08-30 that an externally supplied key is an **input to `request_digest`**, never a value written to `effect_ledger.idempotency_key` (`spec/01` §idempotency_key derivation, `spec/17` §Dedupe consequence 3), so `spec/03`'s four-field derivation stays the only rule. Open: whether any real connector needs the customer's key to be the ledger key, which would require a second registered rule in `03` and a branch in `02` phase 1. | Judge, cross-spec review | Satiya | 2026-09-30 | closed 2026-08-30 — settled: an externally-supplied key is an input to request_digest, never the ledger key (spec/01, spec/17) | Blocks nothing today; blocks the first connector whose upstream dedupes on its own key. |
| OQ-075 | Which document owns the platform's **model-provider** credentials, and how does the proxy inject one for a request that has no `effect_ledger` row? `spec/14` scopes them out and both its injection paths need a ledger row (obligation 2 keys the rewrite entry on the dispatch's `idempotency_key`; `credential_placeholders.effect_key` is `NOT NULL` with a composite FK to the ledger). A model request has none. Until this is decided no document specifies how a model request from inside the sandbox is authenticated. | Judge, cross-spec review; spec/14 and spec/15 reviews | Satiya | 2026-09-30 | decided 2026-08-30 — owner spec/14: the egress proxy is a Phoenix component that holds and injects the model-provider credential keyed on run_token/run_id; a model call is not an effect, so no effect_ledger row is required. spec/14, spec/15 amended | `spec/15` §5's claim was reduced to a requirement on 2026-08-30 and its test row's key half is marked unassertable. Decide before the first run reaches a real provider. |
| OQ-076 | Do bundle nodes carry one behavioural role or several, and is `node_roles_are_content_or_executable` the right strictness? `spikes/04-work-bundle/RESULT.md` says all **133** nodes mapped to **one** of seven roles while its own distribution (108+24+6+6+3+2+1) sums to **150**; `spec/10` declares `roles behavioural_role[]` with `array_length >= 1`. Resolve the arithmetic first: if nodes really are multi-role, the blanket refusal may need a splitting rule instead. | Judge, cross-spec review; spec/10 and spec/16 reviews | Satiya | 2026-09-30 | decided 2026-08-30 — a node may carry several roles (150 role assignments over 133 nodes); node_roles_are_content_or_executable relaxed to forbid only a content role co-occurring with the literal 'executable' role. spec/10 amended; spikes/04-work-bundle/RESULT.md corrected with a dated note | The CHECK landed in `spec/10` on 2026-08-30 and `spec/16` now cites it rather than declaring a compile error. The spike's arithmetic is upstream and unfixed. |
| OQ-077 | Does `tools/validate_spec.py` owe a **citation-resolution gate**? This cohort's line drift moved targets in specs 01, 04, 05, 07, 15 and 16 while six documents cited them by `file:line`, and about a dozen citations landed on unrelated text — a table header, a code fence, an egress section cited for a storage rule. Nothing checks that a `file:NNN` citation still points at what it claims. Proposed shape: assert that a cited line contains a token from the citing sentence, or adopt a convention that cross-document references name sections. | Judge, cross-spec review; spec/13 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — worth building: a file:line citation must resolve to a line containing the quoted token | OQ-043 records the same class of error inside the spikes. The 2026-08-30 fixes converted the worst offenders to section names rather than adding the gate. |
| OQ-078 | Where are **credential and routine administration** audited? `run_events` requires a `run_id` with an FK to `runs`, so creating or revoking a `Credential` (`spec/14`) and pausing or disabling a routine (`spec/11`, which keeps only `state_changed_by` / `state_changed_at`) have nowhere to land. Either a tenant-scoped admin audit log exists — a new table, and therefore a decision rather than an invention — or `spec/06`'s admin routes carry their own record. | Judge, cross-spec review; spec/11 and spec/14 reviews | Satiya | 2026-09-30 | decided 2026-08-30 — new admin_events (tenant_id, principal_id, action, target_kind, target_id, detail, at) table for credential and routine administration. spec/01, spec/04 amended | Non-negotiable 10 wants refusals aggregable, and a route with no durable record cannot satisfy it. |
| OQ-079 | How is a **platform-rule** refusal recorded, given `policy_decisions.policy_id` is `NOT NULL` with an FK to `policies`? `spec/16` names three refusals with no Cedar policy behind them (the `app`-scope default deny, the `temp:` refusal, the scope checks) and `spec/17` names nine messaging refusals that reach no row at all. A log event is not an aggregable decision row. Does the platform need a synthetic `policies` row, or a nullable `policy_id`? | Judge, cross-spec review; spec/16 and spec/17 reviews | Satiya | 2026-09-30 | decided 2026-08-30 — seed reserved policies rows platform:<rule> per tenant at provisioning, so a platform-rule refusal is aggregable through policy_decisions like any Cedar verdict. spec/01 amended | Spans specs 01, 04, 16 and 17. |
| OQ-080 | Who owns the term **`closure selection`** and its wire shape? `spec/12` §8 names it as the projection of `manifest.json` that `Start.config` carries, together with the `node_outside_closure` refusal, and flags in-text that no ADR, no `spec/00`-`09` section and no owning document defines either. `Start.config` is opaque to the control plane by design, so its byte layout cannot be pinned in `spec/07` — but a term two documents depend on and none defines is a new core noun by another name. | spec/12 review | Satiya | 2026-09-30 | decided 2026-08-30 — closure selection is owned by spec/16, beside manifest.json. spec/16 amended, spec/12 cross-referenced | Candidate owner: `spec/16`, alongside `manifest.json`. |
| OQ-081 | Should there be a **fourth knowledge layer** between `tenant` and `agent`? `spec/00`'s owed list named `bundle → team → persona`; neither `team` nor `persona` is a noun the boundary or the ADRs define, and `policies.scope = 'project'` is already an enum value with no v0.1 referent (`spec/01`, exception 2). `spec/16` ships three layers and records the gap. Decide together with whether `Project` becomes a v0.1 resource, since the two enum values would share a referent. | Judge, cross-spec review; spec/16 review | Satiya | 2026-09-30 | decided 2026-08-30 — owner override: add team and persona knowledge layers now (bundle -> tenant -> team -> persona -> agent; resolution falls back persona -> team -> tenant -> bundle); team is a principal-group id, persona is a role label, both new nouns introduced by this decision. spec/16 amended |  |
| OQ-082 | **OQ numbering across parallel spec authors.** Twelve numbers were claimed by three documents in the 10-19 cohort with three collisions, none filed here, so each document's internal references pointed at numbers another document also used. Nothing allocates. Should spec authors use only the bare `unknown — OQ` form and leave numbering to the integrator, plus a `check_open_questions()` that fails when a spec cites an `OQ-NNN` the register does not contain? | Judge, cross-spec review | Satiya | 2026-09-30 | open — at build (2026-08-30) — `make oq` allocates the next open-question number | The 2026-08-30 integration renumbered the cohort contiguously into OQ-071..OQ-141 and rewrote every in-text reference. The mechanism is still absent. |
| OQ-083 | May a connector auto-register a `Resource` on first sight, or must every `Resource` exist in the registry before an `Action` may target it? `spec/10` requires the row via FK but does not say who may create it. Auto-registration would let an agent's first call define `environment` and `classification`, which is exactly the agent-supplied-authority hole the registry exists to close. | spec/10 review | Satiya | 2026-09-30 | decided 2026-08-30 — owner override: yes, a connector may auto-register a Resource on first sight; creation is a ledgered, policy-evaluated effect of kind resource_registration. spec/10 amended | Decide before the first connector ships. |
| OQ-084 | Signing-key lifecycle for bundle and verifier publishers: rotation, revocation, and what a revoked `signing_key_id` means for a bundle a live run has already pinned. `spec/10` stores `signature` and `signing_key_id` and verifies at registration only. | spec/10 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — same argument as spec/09 section 2 (no revocation in v0.1); trigger: first key compromise or first multi-tenant deployment | `spec/09` 2 decided v0.1 ships no definition-revocation path; the same argument probably applies but has not been made. |
| OQ-085 | The **verifier calling convention**: what `verifiers.interface` names (the column carries `'attest/v1'` as an example with no evidence behind it), and how a grader is invoked against a completed `Action`. If the convention lands differently the column is wrong; if no convention is owed the column should be dropped. | spec/10 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — decide with the first customer bundle (attest/v1 = OKF attester contract, sql_equality.py is the instance) | Decide by the first customer bundle. |
| OQ-086 | Who supplies the **independent verifier publisher** in practice: a first-party registry we operate, or one the customer nominates? The `verifier_published_by <> bundle_published_by` CHECK holds either way, so this is a product question — but if we are always the second publisher, the independence guarantee is only as strong as our own review. | spec/10 review, from spike 04 finding 1 | Satiya | 2026-09-30 | decided 2026-08-30 — owner override: the customer nominates the verifier (second) publisher, not a first-party registry we operate. spec/10 amended |  |
| OQ-087 | How is verifier independence enforced on the **receipt** path? `action_receipts.verifier_digest` names a verifier with no relation to the publisher of the bundle whose executable it graded, so spike 04 finding 1's co-publication is reachable there even though the declared-node path is structurally closed. | spec/10 review | Satiya | 2026-09-30 | decided 2026-08-30 — fixed, with a trigger: action_receipts now enforces that verifier_digest's publisher differs from the bundle's publisher on the receipt path too, not only the declared-node path. spec/10 amended |  |
| OQ-088 | Nothing ties a **re-attempt `Action`** to its predecessor's `external_idempotency_token`. A retry takes a new deterministic key and a new `action_id`, `action_relation` admits only `inspects\|verifies`, and no column, FK or CHECK carries the token forward — so token reuse across a re-attempt is a connector convention, not a platform guarantee. Deciding means either a `supersedes_action_id` FK with a registration-time equality rule, or accepting the convention explicitly. | spec/10 review | Satiya | 2026-09-30 | decided 2026-08-30 — added 'retries' to action_relation, with relates_to_action_id naming the predecessor, open to any effect_class rather than only 'observation'. spec/10 amended | Decide before the first `idempotent_mutation` connector ships. |
| OQ-089 | Can `Action` **narrowness** ("no operation may mean do X and then wait until Y") be enforced mechanically? `actions.operation` is open `TEXT` with no CHECK, FK or grant constraining it; the rule is stated as a bundle-registration check on the operations an executable declares, and registration is a surface `spec/06` defines only partially. | spec/10 review | Satiya | 2026-09-30 | closed 2026-08-30 — no; narrowness is enforced at bundle-registration review, stated in spec/10 |  |
| OQ-090 | What does an **`event` trigger** observe? v0.1 has no metric or observation resource for a predicate to bind to, so `trigger_kind='event'` is reserved in the enum and refused `422 trigger_kind_unsupported`. Decide the observable surface (OTel metric? effect ledger? run outcome?) before the kind is enabled. | spec/11 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — v0.1 observable surface is cron + webhook + manual only; trigger: the first mutation-heavy pack (SRE deferred 2026-08-30) (metric spike) |  |
| OQ-091 | May a routine ever run **concurrently with itself**? `overlap_policy='allow'` is reserved in the CHECK and refused `422 overlap_policy_unsupported` because it has no precedent in the study and no defined behaviour. Decide what `allow` would mean (unbounded? a max-concurrency integer?) before enabling it. | spec/11 review | Satiya | 2026-09-30 | closed 2026-08-30 — skip is the only v0.1 overlap_policy; allow stays reserved and refused |  |
| OQ-092 | Should `task_firings` become a first-class **resource** in Omnigent's `scheduled_task_runs` shape (`db_models.py:1546 @ ba9e371`)? `spec/11` makes it a ledger row with no state machine and no API identity; a customer wanting to retry a specific missed tick would need it to be a resource. | spec/11 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — task_firings stays a ledger row, not a first-class resource, in v0.1; trigger: first customer asking to retry a specific missed tick |  |
| OQ-093 | Where is the **pinned tzdata release** recorded, and how is a release change gated? `spec/11` requires every scheduler replica to resolve IANA zones against one pinned release, recorded alongside `canon_profile`, because two replicas straddling a tzdata change derive two names for one tick. Neither `spec/03` nor the deployment story names the pin today. | spec/11 review | Satiya | 2026-09-30 | decided 2026-08-30 — pin the tzdata release in the deploy image; record it in synthesis/scope.yaml's tzdata key, gated at build. spec/11, synthesis/scope.yaml amended |  |
| OQ-094 | Should a routine be able to **pin a definition digest**? Today a routine binds `agent_id`, so an agent repointed at 09:59 changes what the 10:00 tick runs. A pin would make a routine reproducible but would create a second pin authority alongside `runs.pinned_definition_digest`. | spec/11 review | Satiya | 2026-09-30 | closed 2026-08-30 — no; the pin is captured once at Run creation, one authority |  |
| OQ-095 | Should the platform **auto-pause or disable a routine after N consecutive failed Runs**? v0.1 has no system transition on `tasks`. Letta has precedent for a system-cancelled cron task (`cron-file.ts:26-43 @ 852ca24`) but nothing maps to a failure threshold, and inventing one has no evidence. | spec/11 review | Satiya | 2026-09-30 | decided 2026-08-30 — auto-pause a routine after 3 consecutive failed Runs (a system transition, owner notified via an admin_events row); resume is an admin act. spec/01, spec/11 amended |  |
| OQ-096 | May a task carry **more than one trigger** (nightly AND webhook)? v0.1 allows exactly one; a multi-trigger task changes the `firing_key` derivation, since two triggers can name the same instant. | spec/11 review | Satiya | 2026-09-30 | closed 2026-08-30 — one trigger per task in v0.1 |  |
| OQ-097 | What is recorded when an outage exceeds the **24-hour catch-up horizon**? Ticks older than the horizon are neither fired nor enumerated, so the firing history has a hole and the hole is the only evidence. A single summary row would make the gap explicit. | spec/11 review | Satiya | 2026-09-30 | decided 2026-08-30 — one task.catchup_skipped {from, to, count} admin_events row per outage beyond the 24-hour catch-up horizon. spec/01, spec/04, spec/11 amended |  |
| OQ-098 | **Per-firing budget.** Omnigent caps a scheduled task with `max_cost_usd` (`db_models.py:1434 @ ba9e371`) and v0.1 cannot, because enforcement is out of Tier 3. A runaway routine is currently bounded only by its Runs' deadlines. Revisit once `spec/18`'s measurement is live. | spec/11 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — per-firing budget is enforcement, after cost measurement lands (spec/18); trigger: first runaway. See merged OQ-016 | Related: OQ-016 (no infrastructure-level quota anywhere in the study). |
| OQ-099 | Does a **webhook trigger** need its own signing secret and an unauthenticated endpoint? v0.1 makes a webhook fire an ordinary admin-authenticated `POST /v1/tasks/{id}/fire`. A real customer webhook source usually cannot present an admin token. | spec/11 review | Satiya | 2026-09-30 | decided 2026-08-30 — owner override: a per-task API key (tasks.fire_key_ref) on the webhook fire endpoint, not an HMAC secret; held via spec/14's KMS-reference storage; the route is otherwise unauthenticated. spec/06, spec/11, spec/14 amended | Interacts with OQ-075 and `spec/14`'s scope. |
| OQ-100 | **Task-level retry.** ADR-0002 records Task-level and Run-level idempotency as different problems, and `spec/11` answers only the firing side (one tick, one Run). Decide whether "retry this intent" is a first-class operation or stays "a new firing with a new key". | spec/11 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — task-level retry stays 'a new firing with a new key' in v0.1 (ADR-0002); trigger: first customer asking to retry a tick |  |
| OQ-101 | No **step-preserving compaction** strategy exists or is verified. `spec/12` §4 ships compaction `unsupported` and fails a context-exhausted run loudly, which is a refusal rather than an answer: a long-running agent needs one. What strategy, and what negative control proves it cannot move `step_id` or drop an unsettled `ToolCallPart`? | spec/12 review | Satiya | 2026-09-30 | open — spike 08 gate (2026-08-30) — the one real open engineering problem in spec 12: step-preserving compaction | Spike 06 S10 NOT VERIFIED; Pydantic AI teardown S6 `undefined`. |
| OQ-102 | **Nested/delegated agents are untested** against the toolset boundary (spike 06 S9 NOT VERIFIED). `spec/12` §6 rules that a delegation is a platform-created child Run, never an in-process sub-agent, but no gate proves the SDK cannot construct a sub-agent inside the harness process with a toolset the platform did not supply. Needs a gate shaped like gate 13's `LeakyHarness` control. | spec/12 review | Satiya | 2026-09-30 | open — spike 08 gate (2026-08-30) — S9: nested/delegated agents against the toolset boundary |  |
| OQ-103 | **Retrieval authorisation** before content reaches a model or an embedding index is unspecified. `spec/12` §8 constrains WHICH nodes may enter context but not WHO may see a node's content; spike 04 names this the largest untested area. | spec/12 review | Satiya | 2026-09-30 | decided 2026-08-30 — owner spec/16: a Cedar read decision on node classification filters the closure before content reaches the model, one paragraph, marked to be written in full before the analytics pack. spec/12, spec/16 amended | Likely owner: `spec/10` or `spec/16`, not `spec/12`. |
| OQ-104 | The derivation of the harness's **`payload_schema_digest`** is asserted, not verified. `spec/12` §9 derives it from a declared payload-schema document naming the SDK label and envelope version, deliberately excluding the pinned file digests. Nothing yet proves that a patch-level SDK change altering `all_messages()` serialisation forces a digest change — that the `incompatible` guard actually fires. | spec/12 review | Satiya | 2026-09-30 | open — spike 05 gate (2026-08-30) — one assertion: two harness builds, identical schema doc, differing code, same payload_schema_digest |  |
| OQ-105 | The tool boundary is closed against a **hostile in-process caller** only by construction, never by test. Spike 06 scopes its invariant to a cooperating caller; the process split removes the Python object from the driver's reach, but no gate drives a real Go driver against a deliberately hostile Python harness in one container. | spec/12 review | Satiya | 2026-09-30 | open — spike 05 gate (2026-08-30) — the process split is the guard; gate it there against a hostile in-process caller | Likely owner: `spec/13` plus a spike. |
| OQ-106 | Can **`grpcio` serve the `Adapter` service on an inherited, already-connected fd**? `spec/13` §2 specifies a length-prefixed record framing because a gRPC server binds an address rather than adopting a connection, and nothing in this repository has measured the alternative. If grpcio can adopt the fd, the record framing is removable and `spec/07`'s transport sentence covers both halves unchanged. | spec/13 review | Satiya | 2026-09-30 | open — spike 05 gate (2026-08-30) — if grpcio cannot adopt an inherited fd, the length-prefixed framing in spec 13 section 2 stands | Owner: spike 05. |
| OQ-107 | Which **capability-registry key** declares "this adapter survives the destruction and re-creation of its own process", and where does that registry live? `spec/07` names only `effect_ledger_participation`, and the registry `spec/08`'s declaration-completeness check enumerates is not written down anywhere — so that check is unrunnable until it exists. | spec/13 review | Satiya | 2026-09-30 | decided 2026-08-30 — resumable_after_process_loss added as a second declared capability key alongside effect_ledger_participation. spec/07 amended |  |
| OQ-108 | The **inherited socketpair is unmeasured under gVisor**. Spike 05 T2 proved a *filesystem* Unix socket works within one container at `0600`; the inherited-pair form, the fd-3 convention, the record framing, and the 4 MiB ceiling on the encoded `Start` are all owed evidence. | spec/13 review | Satiya | 2026-09-30 | open — spike 05 gate (2026-08-30) — gate the inherited socketpair alongside gates 1-4 | Naturally a spike 05 gate alongside gates 1-4. |
| OQ-109 | Which `tenant_id` does the **`adapter_contracts` registration row** carry, and which component writes it? The key is `(tenant_id, identity, digest)` and the repo rule puts `tenant_id` in every composite key, but registration happens once per driver image at deploy time, when no tenant is named. One row per tenant at first use, or a single platform-tenant row every tenant reads? And is the writer the deploy pipeline or the control plane on first sight of an image? | spec/13 review | Satiya | 2026-09-30 | decided 2026-08-30 — adapter_contracts gets one row per tenant, written by the deploy pipeline at provisioning from the driver image manifest. spec/01 amended |  |
| OQ-110 | Which **KMS** backs `credentials.secret_ref` and `credential_issuances.material_ref`, what is the envelope format, and is the data key per tenant? `spec/14` constrains only the URI shape (`kms://%`) and that the application role holds no decrypt authority. | spec/14 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — customer-cloud KMS, per-tenant data key, envelope = KMS-wrapped DEK + AES-GCM; detail at build | Blocks implementation of the Exchanger. |
| OQ-111 | How does an interactive **OAuth consent** surface in the run state machine? AgentCore's `USER_FEDERATION` uses `on_auth_url` plus a poller, but `spec/05` admits `waiting_input → running` only when a terminal `approvals` row exists, and a consent is not an approval. Either consent gets its own waiting mechanism or `user_federation` ships non-interactive in v0.1. | spec/14 review | Satiya | 2026-09-30 | decided 2026-08-30 — owner override: user-federated OAuth consent is OUT of scope for v0.1; no consent kind added to approvals, no run-state-machine change. Nothing touched by design (111 is OUT) |  |
| OQ-112 | The **placeholder TTL** and the lease-headroom constant in `spec/14`'s pre-claim ordering (`expires_at <= now + effect_lease_duration + headroom`). Both are numbers no evidence in the repo fixes; they need a spike against a real provider's token latency. | spec/14 review | Satiya | 2026-09-30 | open — spike 07 gate (2026-08-30) — placeholder TTL and lease headroom, egress |  |
| OQ-113 | Is **one `egress_host` per `Credential`** too coarse? The single-host column is what makes the leak guard bindable, but a provider with several API hosts (upload vs API vs CDN) would need several `credentials` rows sharing one `secret_ref`, which weakens revocation atomicity. | spec/14 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — one egress_host per Credential stands; trigger: first multi-host provider |  |
| OQ-114 | Is `credential_placeholders_one_live` the right shape for a legitimately **multi-request effect**? `spec/14` assumes one authenticated request per platform-executed effect. An effect that must make a paginated or multi-leg call to one host would need either a fresh placeholder per leg (impossible under the partial unique index) or a placeholder that is not single-use (which drops the blast-radius bound). | spec/14 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — one authenticated request per platform-executed effect stands; trigger: first paginating connector, likely N placeholders per claim |  |
| OQ-115 | Does the **egress guard** connect to the address it checked, or re-resolve, and does it enforce a scheme allow-list before resolution? `spec/15` rules E1 and E2 impose both; `ADR-0009`:53 records only "protocol validation" and "hostname→IP resolution" and enumerates no schemes and no connect-pinning. Both rules are imposed rather than inherited. | spec/15 review | Satiya | 2026-09-30 | open — spike 07 gate (2026-08-30) — read _ssrf.py, then gate the connect/re-resolve behaviour | Probe: read `_ssrf.py @ b48ee38` for the scheme list and the connect path. |
| OQ-116 | Who **re-verifies the cloud-metadata blocklist**, on what cadence, against what source, and do the non-`fd00:ec2::254` rows have IPv6 forms? It is a point-in-time copy of an upstream list read 2026-08-26 with no upstream feed behind it, and that source enumerates exactly one IPv6 equivalent, not one per row. | spec/15 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — re-verify the cloud-metadata blocklist every release, source pinned |  |
| OQ-117 | What is the shape of the **provider health signal**? `ADR-0016`:37 says "a health signal" and nothing more: poll vs push, and what a provider may report between healthy and gone. `spec/15` §1 declares `Report{Gone, Detail}` and reads one bit. | spec/15 review | Satiya | 2026-09-30 | closed 2026-08-30 — decided in spec/15 section 7: the plane polls Health; no richer signal | No longer gates liveness: `spec/15` §7 polls `Health` on a colliding `Create` and treats an unanswered poll as `Gone`. |
| OQ-118 | Does every customer cluster's **CNI actually enforce** the default-deny egress policy `spec/15` §5 depends on? Spike 05 proved gVisor RuntimeClass admission on k3s and tested no NetworkPolicy at all. If a CNI silently ignores the policy, the egress boundary degrades from structural to conventional and the blocklist stops being unbypassable. | spec/15 review | Satiya | 2026-09-30 | open — spike 05 gate (2026-08-30) — add the policy-enforcement (CNI default-deny) assertion to gate 1 | Probe: add a policy-enforcement assertion to spike 05 alongside gate 1. |
| OQ-119 | What are the **warm-window, warm-pool, image-pre-pull and resource-quota values**? `ADR-0016`:80-82 makes the first three scheduler configuration and names no default for any; `CreateSpec.Limits` carries the quota fields with no values. | spec/15 review | Satiya | 2026-09-30 | decided 2026-08-30 — provisional, unmeasured defaults: warm window 10 min, warm pool 2 per WorkerPool, pre-pull via a DaemonSet, sandbox quota 1 CPU / 2 GiB. decisions/ADR-0016 amended (config table); spec/15 amended | Needed before spike 05 gate 1 can assert "idle costs nothing" against a number. |
| OQ-120 | **Deletion across knowledge layers.** v0.1 has whole-file override and no tombstone, so a tenant cannot suppress a bundle file, only replace it. Does a higher layer need a removal mechanism, and if so is it a manifest deny-list or a per-path tombstone file? | spec/16 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — v0.1 ships whole-file override only, no deletion across knowledge layers |  |
| OQ-121 | **Knowledge package limits.** Maximum package size, file count and single-file size are unspecified. Spike 04's real bundle is 133 nodes, which is a scale datum and not a limit. The compiler rejects rather than truncates; it has no bound to enforce today. | spec/16 review | Satiya | 2026-09-30 | decided 2026-08-30 — provisional package limits: 64 MiB per package, 10,000 files, 8 MiB per file; configuration, not schema. spec/16 amended |  |
| OQ-122 | **Agent-authored knowledge.** Letta's agents edit their own git-backed memory with a required `reason` on every mutation and get provenance for free. v0.1 makes the package read-only at run time. What would pull a writable knowledge path in, and would it be a commit-per-turn or a separate mutable layer? | spec/16 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — agent-authored knowledge is Recall-adjacent; same trigger as Recall (a user needing cross-session semantic recall files cannot serve) |  |
| OQ-123 | May a run write **`user`-scoped state for a principal in its delegation chain**? `spec/16` restricts it to `scope_key = runs.created_by`. `principals.on_behalf_of` exists, and Letta's cross-agent guard allows exactly `{self, parent}`, which is precedent for widening it to the delegation parent. | spec/16 review | Satiya | 2026-09-30 | decided 2026-08-30 — user-scope state is writable for {self, parent} (parent = principals.on_behalf_of), Letta's cross-agent guard. spec/16 amended |  |
| OQ-124 | What is the default **retention horizon** for `channel_envelopes`, and is it per tenant or per channel? `spec/17` states the binding rule (no correctness predicate may read the table) but refuses to pick a number, because none is evidenced. | spec/17 review | Satiya | 2026-09-30 | decided 2026-08-30 — envelope retention 90 days per tenant, default. spec/17 amended | Blocks storage sizing, and the audit statement "past the horizon, what was withheld from whom is unanswerable". |
| OQ-125 | Does messaging owe entries in `spec/04`'s closed **event registry**? Non-negotiable 10 requires refusals to be as observable as successes, and `spec/17` defines nine refusal codes that currently reach no run event. Writing new event types is `spec/04`'s to decide, not `spec/17`'s. | spec/17 review | Satiya | 2026-09-30 | decided 2026-08-30 — one message.refused {code} event, registered in spec/04's closed set, appended when the refused post names a run_id. spec/04, spec/17 amended | Related: OQ-079. |
| OQ-126 | Where does **channel membership** live? `not_a_member` and the `audience` check both need a durable membership record; AG2 keeps passports in hub memory and `spec/01` has no such table. Candidate: `channel_members (tenant_id, channel_id, principal_id, role)` — but that is a new noun and needs a decision, not an invention. | spec/17 review | Satiya | 2026-09-30 | decided 2026-08-30 — new channel_members (tenant_id, channel_id, principal_id, role) table. spec/01, spec/17 amended |  |
| OQ-127 | **Channel-lease TTL and renewal interval.** `spec/02` tunes the run lease (60s TTL, renewed every 20s) and gives no channel figure; v0.1 adopts those defaults on "same mechanism, different resource" rather than invent a second pair. Channel hold times differ from run hold times; measure before fixing a number. | spec/17 review | Satiya | 2026-09-30 | closed 2026-08-30 — adopt the run-lease values (60s TTL / 20s renew); no second tuned pair |  |
| OQ-128 | Is there an **operator-facing audit read** of `channel_envelopes`? `spec/17`'s "audit is wider than delivery" guarantee is reachable today only by a direct table query, and the messaging-role grant now in `spec/01` implies a third, operator role that does not exist. | spec/17 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — operator-facing audit read is a spec/06 endpoint, at build | Owed to `spec/06`. |
| OQ-129 | What did **ADR-0003 and spike 01 mean by `causation_id` as a dedupe/derivation key**? ADR-0003's envelope reads "also the reply dedupe key" and its field table says it "doubles as the dedupe key"; `spikes/01-ag2-storage/RESULT.md` keeps it as "one derivation path". `spec/01` and `spec/17` both amended this on 2026-08-30 on the ground that a key derived from a message field cannot be re-derived past the retention horizon. Confirm the ADR's and the spike's phrasings are the loose ones, or say what derivation path they meant. | spec/17 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — one sentence in ADR-0003 stating the causation_id wording precisely | Filed against ADR-0003 and spike 01, not against `spec/01`, which already agrees. |
| OQ-130 | Does **`genai-prices` publish its dataset in a form consumable without importing the Python package** — versioned bytes, a stable schema, a declared currency — and what is the fetch/extraction path? `spec/18` requires the bytes to be stored, digested and priced against by the Go control plane, which is only possible if the data is separable from the package. Not verified in the study. | spec/18 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — probe genai-prices' data form first; blocks the pricing path, not usage capture | Blocks the pricing path; does not block usage capture. Related: OQ-028. |
| OQ-131 | What is the exact **mapping from the harness's usage object onto the `Usage` frame's seven fields** for providers with token classes we do not carry (audio, reasoning, tool-search)? A provider class with no field is silently dropped, which is an under-count that looks like a total. | spec/18 review | Satiya | 2026-09-30 | decided 2026-08-30 — owner override: enumerate the token classes Pydantic AI 2.35.0's usage object exposes as named fields (input_audio_tokens, cache_audio_read_tokens, output_audio_tokens) on the Usage frame, rather than a catch-all field; a new class is a frame change. spec/18 amended | Owner: the Pydantic AI adapter mapping (`spec/12`). |
| OQ-132 | Should an **unpriced pinned model be refused at run admission** (`draft → queued`)? That is the one position where failing closed prevents the spend rather than merely refusing to report it. Deciding yes requires a precondition row in `spec/05`'s transition table and a snapshot lookup before the pin comparison. | spec/18 review | Satiya | 2026-09-30 | decided 2026-08-30 — owner override: do not refuse an unpriced model at run admission; run it and record unpriced usage exactly as any other unpriced call. spec/18 amended | `spec/18` does not specify it because a run-admission gate is arguably enforcement, which is outside Tier 3. |
| OQ-133 | Is spend **rolled up along a delegation chain** (`principals.on_behalf_of`), and to what `delegation_depth`? `spec/18` attributes each record to the acting principal and calls the roll-up a query. A customer asking "what did this human's work cost" needs the chain walk defined, including whether a cycle or a depth cap is possible. | spec/18 review | Satiya | 2026-09-30 | closed 2026-08-30 — spend roll-up along the delegation chain is a query; depth is already known | The delegation rules it leans on are themselves marked NOT VERIFIED in `spec/12` §6. |
| OQ-134 | Which **epoch** does a cost aggregate use when a run is rewound and re-run to the same logical step? `spec/18` sums every epoch because the tokens were really burned, so a rewind-and-retry double-counts a step that produced one business outcome. Correct against the provider invoice, wrong against "what did this step cost". Are those two published figures? | spec/18 review | Satiya | 2026-09-30 | closed 2026-08-30 — sum every epoch: tokens burned are burned regardless of rewind; expose per-epoch too | `cost_records` carries no `epoch` column, so the answer must be reached through the `run_events` join. |
| OQ-135 | Which **rounding mode** at the nano, and does an independent oracle exist for it? `spec/18` decides half-up. No project in the study evidences a choice, so it is a decision and not a finding — and `spikes/VERIFICATION-RULES.md` rule 5 binds the test row to an oracle that must not be the pricing function under test. | spec/18 review | Satiya | 2026-09-30 | closed 2026-08-30 — half-up rounding at the nano; oracle is a decimal test vector |  |
| OQ-136 | Is a **platform-scoped `price_snapshots` with a tenant-scoped pin** the better shape than the tenant-scoped table `spec/18` specifies? Non-negotiable 1 forces the tenant-scoped key; the bytes are identical across tenants, so the cost is duplication and the visible consequence is that two tenants may price the same model against different snapshots on the same day. | spec/18 review | Satiya | 2026-09-30 | closed 2026-08-30 — tenant-scoped price_snapshots stands; non-negotiable 1 admits no exception |  |
| OQ-137 | The **SDK's telemetry surface is unpinned**, and one unpinned file carries a load-bearing `spec/19` claim. `_instrumentation.py` (span names), `models/instrumented.py` (`include_content`, the `version` parameter) and `capabilities/instrumentation.py` (`wrap_tool_execute`, the only creator of a tool span) are not among spike 06's 22 pinned file digests. A Pydantic AI bump could rename every span in `spec/19`'s inventory with all gates green, and `spec/19`'s "the SDK emits no `execute_tool` span for a mediated call" is ASSERTED rather than VERIFIED. Extend the pin to 25 files, add a telemetry-surface gate, or add a spike-06 gate that runs a mediated tool call under an in-memory span exporter. | spec/19 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — verify_pin.py --record; propagate the pinned-file count 22 -> 25 | Requires re-running `spikes/06-tool-interception/verify_pin.py` and propagating the count in `spec/07`:280. Owner: `spec/07` / spike 06. |
| OQ-138 | **Metric cardinality ceiling.** `phoenix.policy.decisions` carries `tenant_id` and `policy_id` because per-tenant, per-policy aggregation is a product requirement; the resulting time-series ceiling (tenants × policies × decision × phase × enforced) is unknown, and ADR-0010's Implications demand cardinality discipline on tenant ids specifically. Decide the ceiling and whether `policy_id` survives it. | spec/19 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — measure the metric-cardinality ceiling |  |
| OQ-139 | **Head sampling cannot force an HTTP-level refusal into its own trace** (`spec/06`'s 403/404/409/422), because the outcome is unknown when the SERVER span is created. A collector-side tail sampler would close the gap. Does v0.1 run one, or is `phoenix.api.refusals` plus the error body the whole answer? | spec/19 review | Satiya | 2026-09-30 | closed 2026-08-30 — no tail sampling in v0.1; refusals are counted, not traced |  |
| OQ-140 | The **semconv version** we emit (1.37.0) is stated in the SDK's documentation, which is outside every digest we pin, so it is ASSERTED and not VERIFIED in `spec/07`'s vocabulary. Can a gate establish it — asserting emitted attribute names against a vendored semconv 1.37.0 registry — or is ADR-0010 amendment 2 rule 5 satisfied by an asserted claim? | spec/19 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — vendored GenAI semconv attribute-name list, asserted at build |  |
| OQ-141 | **Per-tenant sample ratios.** v0.1 fixes the root ratio at 1.0 on the reasoning that an unexercised ratio is untested configuration. The trigger for lowering it (volume, cost) and whether it becomes a per-tenant setting are undecided. | spec/19 review | Satiya | 2026-09-30 | open — at build (2026-08-30) — per-tenant sample ratio fixed at 1.0 for v0.1; trigger: volume or cost pressure |  |
| OQ-142 | Observability backend. | Platform discussion 2026-08-30 | spec/19 | — | decided 2026-08-30 — three layers: run-shaped questions answered by the native UI over Phoenix Postgres (the log is the observability); ops telemetry via an OTel Collector to SigNoz (Grafana LGTM the per-customer alternative; backend stays deployment config per spec/19); Langfuse for evals only, fed by export from `run_events` off the hot path with redaction decided at export | The Collector in front is what keeps the backend swappable and is where tail-sampling would live if OQ-139 reopens. Verify SigNoz/Langfuse licence files at the pinned versions at M12. |
| OQ-143 | Content-level guardrails (prompt-injection, PII, toxicity on model input/output) have no decision: structural guardrails are specified (Cedar phases, approvals, ledger, egress guard) but nothing inspects content. State the v0.1 stance explicitly; if adopted, the shape is a policy phase with a pluggable classifier — one refusal vocabulary, recorded in `policy_decisions`, never a second framework. | Gap found 2026-08-30 | ADR-0013 | before AP9 (promotion writes) or any mutation-heavy pack — retriggered 2026-08-30, SRE deferred | open | Candidates to evaluate then: NeMo Guardrails, guardrails-ai, Llama Guard-class classifiers. Analytics pack is read-heavy and policy-gated, so shipping v0.1 without content guardrails is defensible if stated. Owner: Satiya. |
| OQ-144 | Agent-quality evals (regression when an agent's definition or bundle changes). | Platform discussion 2026-08-30 | spec/10, spec/12 | before the second pack | decided 2026-08-30 (shape) — verifier-native golden runs: routines pinned to a definition digest, replayed against recorded logs/TestModel, graded by Verifiers, compared across digests; Langfuse as the optional eval workbench fed by export. Full design owed as a spec when the second pack starts | The primitives (pins, verifiers, routines, append-only log) already exist; the decision is to compose them rather than adopt an eval framework as foundation. |
| OQ-145 | Login stack (human identity). | Platform discussion 2026-08-30 | spec/06, spec/14 | — | decided 2026-08-30 — Ory Kratos + Hydra: Kratos brokers customer IdPs (OIDC native, SAML via a bridge for SAML-only customers); Hydra is the OIDC provider, service-principal issuer (client_credentials) and RFC 8693 STS candidate; the IdP authenticates humans only, every authorising token is minted by Phoenix, IdP subject maps to one principals row; auth screens Phoenix-built (headless flows, Ory Elements to start) | Verify at build: OSS Kratos SAML status, Hydra token-exchange support, licence files at pinned versions. Alternatives assessed 2026-08-30: Authentik (fastest SAML breadth, Python monolith, IdP-shaped not broker-shaped), SuperTokens (tenant-first, no M2M issuer). |
| OQ-146 | **Free-form human input mid-run has no mechanism.** The agent-asks-a-question case (clarification, pick-an-option) is not approvals: spec/07 has an `Input` frame but spec/06 has no northbound route, spec/05's `waiting_input → running` precondition admits only a terminal approvals row, and nothing ledgers what the human said. Proposed shape (2026-08-30): **`ask_user` as a platform-executed tool** — intent in the effect ledger, run parks `waiting_input`, the question surfaces in a queue (UI screen C4), the attributed reply returns as the `ToolResult`; needs a sibling of approvals (a `human_inputs` row or approval `kind='input'`) so the transition precondition covers it, plus the C4 screen and an inline-reply path. | UI screen inventory review 2026-08-30 | spec/05, spec/06, spec/07, ADR-0015 | before AP-γ (the ask surface makes multi-turn input unavoidable) | open | The tool shape keeps every property: platform executes, ledgered, attributed, prompt-injection cannot fake a reply (replies come from an authenticated principal via the API). Owner: Satiya. |
| OQ-147 | **"Never" item 3 forbids *building* a model gateway; it does not say whether one may be *integrated*.** Its stated reason is that provider fan-out is a commodity ("MAF ships 35 provider packages, which is exactly the surface we should not own"), which is a build argument, and the boundary's own INTEGRATE test — permissive licence, our language, independently useful, solves a subsystem we would otherwise invent — is the test nobody has applied to a router. Proposed 2026-09-03: **integrate a router as a library inside the egress proxy, never as a deployed gateway** — Bifrost (Apache-2.0, Go) via its Go SDK, so no new deployment unit exists, `CreateSpec.EgressVia` keeps its single-destination invariant (`spec/15` §5) and no second control plane appears. Decide whether item 3 is amended to say so, or the router refused. | Gateway comparison 2026-09-03 | Satiya | before the first data connection ships | open | Higress assessed the same day and **rejected for v0.1**: Envoy + Istio per tenant data plane is disproportionate for a read-heavy analytics pack, though it is the better answer if one component must later carry both tool and model egress with data-plane-extensible policy. LiteLLM and Portkey not assessed. `synthesis/v01-boundary.md` item 3, `spec/14`, `spec/15` §5 and `spec/18` amended 2026-09-03. |
| OQ-148 | **Bifrost capability asks, none of which are settled by public documentation, and each of which can collapse OQ-147's shape.** (a) Does the Go SDK expose the governance layer — virtual keys, budgets, rate limits — *without* running the HTTP gateway, or is governance reachable only through the binary? (b) Which features are OSS at the pinned version, verified against build tags in the repository rather than the pricing page: clustering, adaptive load balancing, guardrails and the MCP gateway are advertised as enterprise. (c) Does the stated ~11µs added overhead survive embedding, or is it a property of the standalone server? (d) Licence file at the pinned version. | Gateway comparison 2026-09-03 | Satiya | before the first data connection ships | open | If (a) or (b) fails, the only remaining shape is a deployed gateway, which OQ-147 refuses on the single-destination and second-control-plane grounds — so this closes before OQ-147, not after. |
| OQ-149 | **A router in the model path can falsify `spec/18`'s `Usage` frame, which assumes exactly one usage source.** Two rules are needed and neither is written. First: the router is never a usage source — the harness's frame stays the only path, and a router-side record must not become a second one. Second, and sharper: **transparent cross-provider failover would make the frame wrong**, because Pydantic AI reports the `provider` and `model_ref` it was *configured* with, not the one the router actually reached, and an OpenAI-shaped normalisation also flattens `cache_read_tokens`/`cache_write_tokens` — `spec/18`'s own words for that class of loss are "an under-count that looks like a total". Either the router preserves provider-native protocol and never rewrites provider or model without the harness knowing, or failover is off in v0.1. | Gateway comparison 2026-09-03 | Satiya | before the first data connection ships | open | Interacts with OQ-131 (the named token classes) and OQ-147. Bifrost's `/anthropic` route maps to upstream `/v1/messages`; Higress `ai-proxy` converts only when the target is not native. Neither property is verified here. |
| OQ-150 | **Does a router's budget layer close OQ-016/OQ-098, or open a second policy store?** OQ-016 found no infrastructure-level quota in any of the 13 projects and OQ-098 defers per-firing budgets until cost measurement lands; hierarchical budgets keyed `run_token` → virtual key are exactly the missing control-plane primitive. The objection is the one that demoted Agent Control from INTEGRATE to PORT (`synthesis/scope-reconciliation.md` §3): a governance layer with no `Principal` is a second policy store structurally unable to name the principal ADR-0007 requires. Proposed constraint: the router enforces a ceiling the control plane **configures** and is never where a policy is **authored**, and any refusal it produces surfaces in the platform's refusal vocabulary (`policy_decisions`), never its own. | Gateway comparison 2026-09-03 | Satiya | before the first data connection ships | open | This is the strongest single argument for the integration and the strongest single argument against it, and they are the same argument. Merges nothing: OQ-016 and OQ-098 stay open until a ceiling exists. |
| OQ-151 | **Connector hosting has no owner.** Boundary item 4 says customers bring connectors, and OQ-083, OQ-088 and OQ-114 are all written in anticipation of connectors shipping, but no document says what *runs* one. Candidate assessed 2026-09-03: **Docker MCP Gateway** (MIT, Go, one container per server, OAuth and secret handling), placed **downstream of the platform's tool executor** — after `dispatch`, never beside the model path. Three constraints to settle before adoption: (1) its catalogue is discovery and ADR-0012 requires declared, so a catalogue can only be a menu a definition pins a subset from at authoring time, because handshake step 1 requires `tool_name` in the **pinned** definition (`spec/07` §Who executes a tool); (2) it is a runner and never a policy point, so no allow-list configured there may stand in for a decision ADR-0013 expects in `policy_decisions`; (3) its egress traverses `EgressVia` like the executor's own (`spec/07` §Who executes a tool, `spec/15` §5). | Gateway comparison 2026-09-03 | Satiya | before the first data connection ships | open | Operational note: Docker Desktop 4.59+ is the documented dependency, escapable via `DOCKER_MCP_IN_CONTAINER=1` on Docker CE — verify before depending on it. The **refused** shape is the router calling the connector host directly over MCP; `spec/07` amended 2026-09-03 to name it. |
| ID | Deferred | Why | Revisit when |
|---|---|---|---|
| | | | |
