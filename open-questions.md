# Open Questions
<!-- status: final -->

Anything that would otherwise become "we'll figure it out later." Every entry
needs an owner and a decide-by date. This register is what stops the exit
criteria being silently dodged.

Rules:
- A `verdict: unknown` on a deep project belongs here.
- An unresolved conflict between two projects' designs belongs here.
- Closing an entry means linking the ADR or document that resolved it.

## Status key

`open` · `investigating` · `resolved` · `deferred` (with a reason and a revisit date)

---

## Register

| ID | Question | Raised by | Owner | Decide by | Status | Resolution |
|---|---|---|---|---|---|---|
| OQ-001 | Does the LLM gateway (`freellm.kaapibyte.com`) have a durable provider-key configuration, or will availability keep flapping? First probe returned `no_providers_configured`, second succeeded. Affects any tooling that depends on it. | Phase 0 setup | | | open | Non-blocking: no teardown claim may come from a model. |
| OQ-002 | Several shortlist projects may be announcement-only with no readable source. Need substitutes that answer the same architectural question. | Phase 0 planning | | 2026-08-26 | resolved | Phase 1 recon: Google AX (`google/ax`) and Omnigent (`omnigent-ai/omnigent`) are both class A and highly relevant. GroupMind and Agent Control could not be located — downgraded to `recon`, questions reassigned in `synthesis/recon.md`. |
| OQ-003 | G-section (human-agent workspace) had the thinnest expected evidence after GroupMind could not be located. | Phase 1 recon | | Phase 5 | **partly resolved** | Letta supplies far more than expected: Slack/Discord/Telegram channels with allowed/admin user lists, `dm\|group` scope, `allow\|deny\|pair` access decisions, per-channel permission modes, mention-only mode, thread context, and durable approvals delivered to whichever channel the human is on. G-section now has a strong primary source. Still worth confirming with Omnigent's session sharing. |
| OQ-004 | Letta's source moved to `letta-ai/letta-code`; is the memory architecture still inspectable or has it moved behind Letta Cloud? | Phase 1 recon | | Phase 2 | **resolved** | Fully inspectable, and better than the V1 block store would have been: memory is a git-backed markdown filesystem with kernel-enforced per-agent isolation. Produced the first `first_class` H4 in the study. ADR-0008 confirmed strongly. |
| OQ-005 | Four projects (LangGraph/LangSmith, Letta, AWS AgentCore, Google Agent Platform) keep their control plane closed — exactly the layer we intend to build. Prior art for identity, hosted memory, multi-tenancy and observability backends is thin across the whole study. | Phase 1 recon | | Phase 5 | open | Structural limit of the exercise. Weight Google AX and Omnigent more heavily; both keep their control plane open. |
| OQ-006 | `web_search` / `web_fetch` were unavailable during recon (no local Ollama) and the GitHub search API was IP-rate-limited, so recon relied on direct repo-path probing. A project could have been missed. | Phase 1 recon | | Phase 2 | resolved | **Re-checked 2026-08-26 via npm + PyPI registries.** Agent Control located at `agentcontrol/agent-control` (Apache-2.0, 299★) and restored to `targeted`. GroupMind confirmed absent across GitHub, npm and PyPI. Lesson: package registries beat GitHub search for discovery, since they carry the canonical repo URL. |
| OQ-007 | Does LangGraph Platform add definition-version pinning on resume, or does it inherit the silent work-loss hazard proven in OSS (renamed node → resume returns empty, no error)? Platform is closed source. | LangGraph teardown | | Phase 5 | open | Docs review only; class B evidence at best. Relates to ADR-0011. |
| OQ-008 | What lease/heartbeat mechanism does LangGraph Platform use to detect lost workers? Closed source, so `C8` stayed `unknown`. | LangGraph teardown | | Phase 5 | **partly resolved** | Letta answers the general question well: `SchedulerOwner` with pid + token + `process_start_ticks` + `boot_id` detects stale ownership across PID reuse and reboot (`src/cron/cron-file.ts:45-52 @ 852ca24`). Adopt that pattern; LangGraph's specific mechanism remains unknown. |
| OQ-009 | Is there any accepted pattern for side-effect idempotency across a resume boundary, or is developer discipline the entire answer? | LangGraph teardown | | Phase 5 | open | **Six for six: no project has an idempotency key on tool calls.** Google AX is closest structurally (atomic per-step log appends, idempotent `CreateActor`); Omnigent found and fixed an adjacent lost-update race on usage deltas (`BEGIN IMMEDIATE` / `SELECT FOR UPDATE`, bug #9) but leaves tool effects unprotected. This is now confirmed as a genuine industry gap and a BUILD item: a durable effect-ledger keyed on a deterministic task id. |
| OQ-010 | Does the OpenHands agent-server define crash-recovery semantics for a conversation interrupted mid-action, or is the event log the only contract? | OpenHands teardown | | Phase 3 | open | `C5` left `unknown` for both OpenHands and Letta. |
| OQ-011 | How does OpenHands Cloud enforce the quotas the OSS Docker workspace lacks (no CPU/memory/pid limits)? Closed. | OpenHands teardown | | Phase 5 | open | Relevant to S9, which is unanswered in all three deep projects. |
| OQ-012 | Is ACP's stdio transport sufficient for a genuinely remote agent, or does it force co-location? | OpenHands teardown | | Phase 3 | **resolved** | Google AX answers it: `HarnessService.Connect` is a gRPC bidirectional stream, so a remote harness needs **no co-location** (`proto/ax.proto:85-91 @ b777313`). Implication: our southbound API should be gRPC-shaped with ACP as one adapter *behind* it, not as the transport. Recorded as an amendment on ADR-0006. |
| OQ-013 | Does any project have agent versioning or revocation? | Letta teardown | | Phase 3 | **partly resolved** | **Versioning: yes, once.** Omnigent has a monotonic `Agent.version` incremented on every bundle update over a content-addressed tarball (`stores/agent_store/sqlalchemy_store.py:300 @ ba9e371`) — the only agent versioning in six projects. **Revocation: still absent six for six** — Omnigent hard-deletes agents, though its *device grants* have a real revocation lifecycle. Combined with OQ-020, the lesson is that versioning without a run-side pin does not prevent mid-flight upgrade. |
| OQ-014 | How does git-backed memory behave at scale — repository growth, history rewriting, conflict frequency under unattended operation? | Letta teardown | | Phase 5 | open | Affects whether we borrow the pattern or only the ideas. |
| OQ-015 | Is Letta's `pair` (pending sender authorisation) durable across restart like `PendingControlRequest`, or in-memory only? | Letta teardown | | Phase 3 | open | Minor, but the pattern is one we want to copy. |
| OQ-016 | **S9 (runaway spend) is `undefined` or partial in all three deep projects.** None has a hard cost ceiling; the best available are goal-loop `capped` status (OpenHands) and subagent context budgets (Letta). No infrastructure-level quota anywhere. | Phase 2 synthesis | | Phase 5 | open | Likely a genuine differentiation opportunity. Budget enforcement may need to be a control-plane concern rather than a runtime one. |
| OQ-017 | Does AX intend `Interaction` to become a pollable resource (per its own `TODO`), and would that make it a Task or still a Run? | Google AX teardown | | Phase 5 | open | Closest thing to Task/Run separation found so far; the `TODO` suggests even Google finds this hard to land. |
| OQ-018 | Is Google AX's total absence of authN a "not yet" or a deliberate deployment assumption (service mesh / sidecar auth)? Nothing in the repo says. | Google AX teardown | | Phase 5 | open | Affects whether we read it as an oversight or as a valid architectural stance for a control plane behind a mesh. |
| OQ-019 | How does SubstrATE handle actor eviction under memory pressure, and does a suspended actor expire? Determines whether suspend/resume is a durability mechanism or only a cost optimisation. | Google AX teardown | ADR-0016 | Phase 5 | open — parked (2026-08-27: v0.1 provider is gVisor checkpoint-and-kill; revisit only if a provider change is proposed) | External dependency (`github.com/agent-substrate/substrate`), not read. **Now blocks ADR-0016** (sandbox placement), which cannot move from Proposed to Accepted until eviction and expiry semantics are known: if a suspended actor can expire silently, suspend/resume is a cost optimisation and not a durability mechanism, and the data-plane design changes. Probe: read the SubstrATE source. |
| OQ-020 | Does anything pin `Agent.version` onto a conversation at start in Omnigent, or is the version purely an audit counter? | Omnigent teardown | | Phase 5 | **resolved** | Nothing does. Searched `entities/conversation.py` — no version field. This is the ADR-0011 challenge: versioning without pinning. Recorded as an ADR-0011 amendment (the pin lives on the run, not the definition). |
| OQ-021 | Is Omnigent's `harness_bench` DRIFT reconciliation run in CI, or a manual tool? | Omnigent teardown | | Phase 4 | **resolved** | **Two layers, and the split is the answer.** `tests/harness_bench/test_bench.py` runs an **offline** layer on every PR — registry membership, *profile completeness*, reconciliation semantics, community-profile resolution, matrix rendering — with no network or credentials. The **live** probe layer (which produces real DRIFT verdicts) is gated on `--profile` plus a runnable harness CLI, and asserts P0 dimensions match the declaration. So the *structure* is CI-enforced continuously and the *behavioural claims* are enforced only when credentials exist. Adopted into ADR-0012: split our bench the same way, because it means declaration completeness never regresses even when live probes cannot run. |
| OQ-022 | Why are all four optional capability axes (`steering`, `live_queue`, `images`, `compaction`) unpopulated across all 26 Omnigent harnesses — is the tri-state new, or is populating it blocked on live probing? | Omnigent teardown (verified live, 0.11.0) | | Phase 5 | open | A tri-state nobody fills buys nothing. Drove the ADR-0012 requirement that our bench report *declaration coverage*, not just drift. |
| OQ-023 | Are Omnigent's scheduled task runs claimed with a lease or lock? | Omnigent teardown | | Phase 5 | **resolved** | No. `ScheduledTaskScheduler` is explicitly an in-process RRULE scheduler with "no in-memory schedule state beyond the live timers" (`omnigent/server/scheduled/scheduler.py:1-24`). SKIP overlap and 30s misfire grace guard self-overlap only; **two replicas would double-fire**, and missed fires are deliberately not replayed. Warning for our scale-out control plane: runs need a durable claim (`UPDATE ... WHERE status='scheduled'` on rowcount, or `SELECT ... FOR UPDATE SKIP LOCKED`). |
| OQ-024 | What happens to AG2's dedupe guarantee after terminal-channel pruning clears `_causation_index`? `find_envelope_by_causation` returns `None` "either it was never accepted or its channel has already closed" — conflating "no duplicate" with "cannot tell". | AG2 teardown | | Phase 5 | open | Material to ADR-0014: our effect ledger must not have a retention horizon that silently degrades into "cannot tell". A tombstone or an explicit INDETERMINATE result is needed. |
| OQ-025 | Is there a federation story for WAL consistency across AG2 hubs, given `remote_agent` passports are explicitly "a cache"? | AG2 teardown | | Phase 5 | open | Affects whether our messaging design can span control planes or must assume one authority per channel. |
| OQ-026 | Does AG2's `record_observation` weight recent outcomes? | AG2 teardown | | Phase 5 | **resolved** | No. `ObservedStat` is a **lifetime counter with no decay**, and `p50_latency_ms` "replaces the prior" value — "a single-sample stand-in for a future reservoir", so it is not a percentile at all. `task_id` dedupes double-counting (cascade EXPIRED + owner COMPLETED). Notably, observation **expands** the capability index: an agent appears under a capability "even if it wasn't in their original `claimed_capabilities`", so capabilities can be *discovered*. Gap for us: routing on reputation needs a window or reservoir, since a lifetime counter never forgets an early failure. |
| OQ-027 | Does a durable engine preserve a pending `DeferredToolRequests` across workflow suspension, giving Pydantic AI durable HITL for free? | Pydantic AI teardown | | Phase 5 | **partly resolved** | Found two composable modes rather than one. Default: the run *returns* `DeferredToolRequests` and the caller owns pending state. Alternative: `HandleDeferredToolCalls` resolves inline, and a handler may **decline by returning `None`** so the next handler in the chain tries, with unhandled calls bubbling up as output. That chain-with-fallthrough is worth copying — policy auto-approves what it recognises, humans get the remainder. Whether a durable engine persists the pending value across suspension is still unverified. |
| OQ-028 | Is `genai-prices` maintained on a cadence that makes it safe to depend on for budget *enforcement*, rather than only reporting? | Pydantic AI teardown | | Phase 5 | open | Relevant if we adopt it for ADR-0013 cost gates. Note Pydantic AI itself only *measures*; Omnigent enforces, and fails closed when a model has no price — that combination (external price data + fail-closed on unpriced) is probably the right design. |
| OQ-029 | Does any ADK session backend enforce `(app_name, user_id)` scoping at the storage layer, or is it a query convention? | Google Agent Platform teardown | | Phase 5 | **resolved** | **Enforced.** `StorageSession` declares `app_name`, `user_id` and `id` all `primary_key=True` (`sessions/schemas/v0.py:146-161 @ 85b52f6`), so a lookup that omits or mistakes the scope finds nothing. Same mechanism as Omnigent's `workspace_id` — **second independent convergence on tenancy-in-the-primary-key**, which settles the approach for our design. Caveat: nothing authorizes the caller's claim to a `user_id`, so this prevents accidental leaks and not deliberate impersonation — which is exactly the principal/credential split now recorded on ADR-0007. Process note: I initially wrote "convention, not enforcement" and had to correct it by reading the schema. |
| OQ-030 | Does Vertex AI Memory Bank support TTL or deletion, given `custom_metadata` names TTL as a service-specific field that "may later become first-class"? | Google Agent Platform teardown | | Phase 5 | open | Relevant to `H6` (knowledge expiry), which is `implicit` at best everywhere. The interface anticipates expiry without specifying it, so our Recall interface should include TTL from the start rather than as metadata. |
| OQ-031 | Does HumanLayer's daemon reconcile approvals on restart — can a tool call whose approval was decided while the daemon was down still proceed? | HumanLayer teardown | | Phase 5 | **resolved** | **Yes, and it is a named abstraction.** `session.ApprovalReconciler` is documented as an "interface for reconciling approvals after session restart", with `ReconcileApprovalsForSession(ctx, runID)` called from three sites in `session/manager.go` (:522, :1849, :2067). On session start it waits 2s (cancellable) for the session to come up, then reconciles by `run_id`, and **logs on failure rather than failing the session**. That fail-open choice is sound *because* the approvals are durable: a failed reconcile leaves the pending row intact for the next attempt, whereas failing the session would strand work. Shape adopted into ADR-0015 rule 1: reconciliation keyed on the run, triggered by restart, non-fatal. |
| OQ-032 | Does HumanLayer's hosted (commercial) service record an approver identity, making the audit gap a local-only limitation? | HumanLayer teardown | | Phase 5 | open | The OSS daemon's `approvals` table has no `decided_by`. If the hosted service adds one, the gap is a single-user artefact rather than a design position — but either way **our** approval must name its principal (ADR-0015 rule 4). Not blocking: the decision is made on requirements, not on their precedent. |
| OQ-033 | Does AgentCore's Cedar integration expose policy *analysis* (equivalence, permissiveness comparison), or only evaluation? | AWS AgentCore teardown | | Phase 5 | **resolved** | **Tested, and the answer is partial.** Installed `cedarpy` and ran it: `validate_policies` catches a typo'd attribute name statically (`resource.riskLevel` vs `resource.risk` → `validation_passed=False`), which is the strongest argument for Cedar and better than the one I originally made. But **equivalence / implication / permissiveness comparison are not exposed in the Python binding** — they live in Cedar's Rust/Lean analysis crate. So **static** shadow comparison is a cross-language dependency, not an off-the-shelf capability; **empirical** shadow via `observe` mode is what v0.1 gets. Recorded as ADR-0013 amendment 3. |
| OQ-034 | Is there agent-level revocation in AgentCore — disabling a workload identity outright — rather than only deleting its credential providers? | AWS AgentCore teardown | | Phase 5 | open | The SDK exposes `delete_*_credential_provider` (revoking what an agent can *reach*) but no workload-disable call. If the control plane has one it is the first true `D9` answer in twelve projects; if not, revocation-by-credential is the best available pattern and we should build agent-level revocation ourselves. |
| OQ-035 | Do AgentCore memory namespaces enforce authorization, or is `/actor/Jane/` readable by any caller who names it? | AWS AgentCore teardown | | Phase 5 | **partly resolved** | Server-side authorization is not inspectable, but the SDK narrows the blast radius meaningfully: `build_namespace_params` requires exactly one of `namespace` (exact) or `namespace_path` (prefix) and **refuses wildcards** — `"Wildcards (*) are not supported in namespaces."` (`_utils/namespace.py:18-25`) — so `/actor/*/` cannot sweep every actor's memory. Whether a *named* namespace is authorized against the caller remains unknown. Design conclusion regardless: **pair enforced scopes (ADK's composite key) with namespace hierarchy**, because a path is a convention and a primary key is not. |
| OQ-036 | **Class-level unknown:** 52 of 173 probes are `unknown` for AWS AgentCore because it is a client SDK over managed services — concentrated in `K` sandbox (8), `L` orchestration (7), `B` runtime (6), `A`/`C` (5 each), `P` storage (4). | AWS AgentCore teardown | | Phase 5 | open (accepted) | **Deliberately `unknown`, not `absent`.** The distinction is the methodological spine of this study: I read the SDK surface, not the service implementation, so absence of an API is not absence of a capability. Resolving these would require an AWS account with AgentCore access plus service documentation, which is out of scope. **This is the only project where the evidence class degrades at subsystem scale**, and it is recorded here rather than silently coded as `absent` — which would have overstated nine gaps in the capability matrix. |
| OQ-037 | Is there an escape hatch for a deliberately compatible workflow change in MAF — a way to declare "this edit does not invalidate checkpoints"? A bytecode hash has no notion of a semantically-null edit. | Microsoft Agent Framework teardown | | Phase 5 | open | I searched and found none. **Design conclusion regardless** (ADR-0011 amendment 3): accept the brittleness for v0.1, because a false *incompatibility* costs a restart while a false *compatibility* costs silent corruption of the kind LangGraph exhibits (verified: renamed node → resume returns `[]`, no error, work lost). If an escape hatch becomes necessary it must be an explicit operator assertion recorded on the run, not a loosening of the default. |
| OQ-038 | Does MAF's telemetry follow GenAI semantic conventions with a declared vendor namespace, as ADR-0010 now requires? | Microsoft Agent Framework teardown | | Phase 5 | open | Not audited — this was a targeted pass and `_telemetry.py` was out of scope. Recorded `unknown` rather than assumed. Low stakes for our design: ADR-0010's requirements are already settled from Cloudflare, Pydantic AI and ADK. |
| OQ-039 | How deep is MAF's `hyperlight` integration — is a micro-VM the default execution boundary for tool code, or an opt-in package? | Microsoft Agent Framework teardown | | Phase 5 | open | Matters for ADR-0009's process-boundary half: if micro-VM startup is cheap enough to be the default, that is a stronger isolation baseline than the container/process boundaries used elsewhere in the study. |
| OQ-040 | Is Agent Control's `namespace_key` enforced in the schema, or only a filter parameter? | Agent Control teardown | | Phase 5 | **resolved** | **Enforced, and more strongly than anywhere else in the study.** `namespace_key` is in the composite **primary** key *and* in composite **foreign** keys on the association tables, with the intent stated in a comment: "Composite FKs enforce same-namespace references on both sides" (`server/src/agent_control_server/models.py:41-80`). Omnigent and ADK prevent unscoped *reads*; this also prevents cross-namespace *writes*, so binding a policy in namespace A to a control in namespace B is a constraint violation. For a policy system that is the more dangerous case. **I had recorded it as a filter field after reading only the API models** — third time in this study that the schema overturned an inference from an API surface. |
| OQ-041 | Does Agent Control's `observe` mode support running a candidate control against live traffic alongside an enforced one (true A/B shadow), or only non-blocking evaluation? | Agent Control teardown | | Phase 5 | **resolved** | **True A/B shadow.** All controls bound to a target evaluate **concurrently** under a semaphore, each recording its own `ControlExecutionEvent`, and only `deny`/`steer` affect the outcome (`engine/src/agent_control_engine/core.py:662-780`). So a candidate control runs on the same live traffic as the enforced ones without changing behaviour. **One caveat carried into ADR-0013 amendment 2**: a `deny_found` event cancels remaining evaluations, so observe controls may record no verdict for *denied* traffic — biasing the shadow sample away from exactly the cases a candidate policy most needs comparing on. Our observe evaluations must be exempt from deny-triggered cancellation, accepting the latency cost. |

---

## Deferred with intent

Things consciously not decided in v0.1, recorded so they are not mistaken for
oversights.
| OQ-042 | Read the SubstrATE source (`github.com/agent-substrate/substrate`): actor eviction, suspension expiry, and whether a suspended actor's identity is stable across resume onto a different worker. | ADR-0016 / SaaS repositioning | ADR-0016 | Phase 7 | open — parked (2026-08-27: v0.1 provider is gVisor checkpoint-and-kill; revisit only if a provider change is proposed) | Directly gates ADR-0016 moving to Accepted, and gates spike 05's second gate (tool interception across a sandbox move). Probe: clone and read; do not infer from the AX teardown, which only observes the client side. |
| OQ-043 | Can **Pydantic AI**'s native tool execution be disabled so every tool call crosses the `ToolCall → ledger → ToolResult` boundary? Tools are registered Python functions — the decorator's implementation is `agent/__init__.py:2423 @ b48ee38`, usage documented at `:2460`; `:2399` was cited in error (an `@overload` stub) and the citation was corrected 2026-08-27 — so the boundary should be a **decorator**; that is the claim spike 06 must confirm. | SaaS repositioning; harness decision 2026-08-27 | ADR-0004, ADR-0014 | **resolved 2026-08-27 — SPIKE 06, PASS** | **Answered, and the question was framed wrongly.** The boundary is a **toolset**, not a decorator: `ExternalToolset.call_tool` raises unconditionally (`toolsets/external.py:46 @ b48ee38` *(citation corrected 2026-08-28: the unconditional raise is at `:46`; `:44` is the `call_tool` parameter line)*) while the tool is still advertised to the model (`:36`), so the SDK is **never given an executable body** — nothing to disable, no wrapper to route around. `DeferredToolRequests` ends the run (`_deferred.py:27`) and `deferred_tool_results=` supplies platform results on resume (`agent/__init__.py:1139`). **53 gate assertions** *(18 → 26 → 47 → 53; corrected 2026-08-28 from `spikes/06-tool-interception/RESULT.md`, which `make check` now sums — the count was never re-read after rounds 2 and 3 added gates, and round 4 added six more)*, oracle = a filesystem side channel the harness never touches. The `FunctionToolset` control shows `@agent.tool` **is** the bypass, so Phoenix never uses it. Pin **fixed** at `pydantic-ai-slim == 2.35.0`, established by **nineteen** byte-identical file digests because `uv-dynamic-versioning` + no git tags means the tree cannot state its own version *(amended 2026-08-27, superseding "five": the five-file pin left `native_tools/__init__.py` — the module that decides which vendor-hosted tools may be admitted — unpinned, so review's own control worked again on exactly the module that governs admission)* *(amended again 2026-08-28, superseding "sixteen": that pin omitted `models/__init__.py`, where admission is ACTUALLY decided — `resolve_request_tools` filters native tools against `supported_native_tools` — so the filter could be replaced in a scratch SDK copy with `pin holds: 16 files` and all 47 gates still green; `profiles/__init__.py` and `capabilities/__init__.py` joined it)*. **Amended 2026-08-27 (round 3):** the toolset argument is necessary and was not sufficient, because the harness returned a raw `Agent`; `agent.override(native_tools=[WebSearchTool()])` against `FunctionModel` (which supports native tools, `models/function.py:242-244`) delivered a vendor-hosted tool to the model with no Phoenix involvement. The harness now exposes no public name that is, or returns, an `Agent`, and the effect ledger was rewritten to `spec/01-schema.md:332`'s statuses and `spec/02-consistency.md`'s phase predicates (it had used a status `settled` that the schema does not define, and no run-lease predicate at all). Unresolved and now urgent: `sdk_in_process` "collapses to a function call" cannot hold across a **Go** control plane and a **Python** harness — see OQ-056. Vendor-hosted tools stay unsupported: `ExternalToolset` cannot express them, as there is no local body to withhold. | `spikes/06-tool-interception/RESULT.md` | **Rescoped 2026-08-27 to ONE SDK** (was three: Claude Agent SDK, LangGraph, OpenAI Agents SDK). If native execution cannot be intercepted for a tool, that tool **cannot be registered in v0.1** — `spec/08` row 30c, marked NOT VERIFIED. Vendor-hosted tools (web search, code execution, file search, computer use) execute on the vendor's side; an `observed` ledger status was specified and **retracted** on 2026-08-27 because it promised reporting that cannot hold when the connection drops. Any future admission must be *provider-reported use*, require a durable provider receipt for anything stronger, and carry egress and confidentiality constraints. **Spike 06 scope:** (1) register zero tools and assert no ledger-bypassing path; (2) wrap `@agent.tool` and assert every call produces an `effect_ledger` row *(superseded 2026-08-28: item (2) was never executed as written — wrapping `@agent.tool` is the bypass, not the boundary. The spike declares tools through `ExternalToolset` and additionally exposes no `Agent`; see the answer column.)*; (3) fix the pinned version, which is a placeholder at 2.35.0 until then *(superseded 2026-08-27: the version is no longer a placeholder — it is fixed by SHA-256 file digests (sixteen then; **nineteen** since 2026-08-28) in `spikes/06-tool-interception/pinned_digests.json`, asserted before collection by `conftest.py`)*. |
| OQ-044 | Does an enterprise buyer accept that quorum/separation-of-duties approval is unsupported, or is it a gating requirement for the first deployment? | Multi-party approval refusal | ADR-0015 | Phase 7 | open | Determines whether the focused approval teardown happens before or after first deployment. The refusal is defensible on evidence (one precedent, one `decided_by`), but it is a **narrowed enterprise claim** and must be said out loud in a sales conversation, not discovered in one. |
| OQ-045 | Spike 03's own module docstring still says "the eight scenarios review listed as unverified" (`spikes/03-postgres/test_postgres.py:2`) while the file runs nine (`s1_epoch_append_vs_rewind` … `s9_approver_must_be_human`, `:77-605`) and prints "PASS — all nine scenarios hold against real Postgres" (`:708`). Should the docstring be corrected, or does it read defensibly as history of what review had listed? | Round-2 review, out-of-scope residue | Satiya | 2026-09-15 | resolved 2026-08-27 — docstring no longer hand-counts | The user-facing half of the same stale count was fixed this round (`tools/validate_spec.py`, skip message: eight → nine). Recorded rather than fixed because the docstring is a sentence about what a past review said, and this repo's rule is to amend with a date rather than silently renumber history. |
| OQ-046 | `make check` is not reliably reproducible from the documented quick procedure. (a) The procedure (docker run, wait 5s, `make check`) omits applying `spikes/03-postgres/schema.sql`; without it the gate fails with sqlstate 42P01 undefined_table, and `_apply_spec_ddl()` deliberately rolls back so it never seeds the spike's tables. (b) `make check` runs `tools/test_validate_spec.py` first, whose scratch-copy runs invoke the same validator against the same default DSN, leaving live backends on that database; a subsequent real gate run then failed on `run_events_tenant_id_run_id_fkey`. Should the schema apply be part of the documented procedure, part of the gate, or neither? | Round-2 review, out-of-scope residue | Satiya | 2026-09-15 | open | Observed on an untouched tree from a fresh container: exit 2, exit 0, exit 0, then a run that hung past ten minutes. Run directly, the spike passes twice in a row (42/42), so the spike itself is re-runnable; it is the shared server under the self-test that breaks it. This round only improved the failure MESSAGE (`postgres gate failed: <tail> (did you apply schema.sql?)`); the reproducibility question is untouched. |
| OQ-047 | The Postgres gate has no isolation. The DSN is a single hard-coded database and `fresh()` (`spikes/03-postgres/test_postgres.py:44-56`) wipes a shared fixture (`DELETE FROM ... WHERE tenant_id=1`) that every scenario reuses, so two `make check` runs on one machine corrupt each other. Should the gate create a per-run database or schema, or is a documented "one gate run at a time" rule enough? | Round-2 review, out-of-scope residue | Satiya | 2026-09-15 | open | Measured while a second session shared the container: 3 of 6 fully serial runs failed with three distinct signatures (`TypeError: 'NoneType' object is not subscriptable`, `ForeignKeyViolation ... run_events_tenant_id_run_id_fkey`, `UndefinedTable: relation "effect_ledger" does not exist`). On a private container the same unmodified binary passed 5 of 5. A gate that goes red for reasons unrelated to the artefact teaches re-running until green, which is what VERIFICATION-RULES rule 6 exists to stop. The first of those three signatures was a real defect and is fixed this round (F2: the `k4` fixture precondition). |
| OQ-048 | `DESIGN.md:352` still asserts "We own a thin internal harness over the vendor SDKs" (plural) while ending with the citation `` (`v01-boundary.md:91`) ``, and `synthesis/v01-boundary.md:91` now reads "on **Pydantic AI 2.35.0** (amended 2026-08-27, superseding \"over the vendor SDKs (Claude Agent SDK, LangGraph, OpenAI Agents SDK)\")". Is the plural superseded residue of the two-SDK plan, or a deliberate statement that the adapter boundary keeps a second SDK possible? | Round-2 review (consistency R3-01, gate N2), closing judge | Satiya | 2026-09-15 | resolved 2026-08-27 — DESIGN.md:352 now says Pydantic AI | Raised by two independent lenses at medium. The contradiction was **created by round 2**: before `73fc21e..b4cef6e` both sentences said "over the vendor SDKs" and agreed; round 2 corrected the boundary row (F1) and re-anchored this citation `:87` → `:91`, so the claim now cites the line that declares its own wording superseded. `DESIGN.md` carries no "the boundary wins" precedence clause, unlike `synthesis/reference-architecture.md:3`, so nothing governs the disagreement. `DESIGN.md:35-36` and `:117` already carry the corrected claim, so the document also contradicts itself. Not fixed by the judge: the residual finding is medium, not blocking, and the closing mandate fixes only blocking one-line residue. |
| OQ-049 | `spec/01-schema.md:126` carries the same plural inside normative DDL — `-- a thin internal harness over the vendor SDKs, so there is no third-party` — as an undated active claim. Should it be amended with a date like `v01-boundary.md:91` and `DESIGN.md:35` were? Answer together with OQ-048. | Round-2 review (consistency R3-02, gate N3) | Satiya | 2026-09-15 | resolved 2026-08-27 — spec/01:126 now says Pydantic AI | Untouched by either round, so outside both rounds' scope. No gate can reach it: `_scanned_files()` does include `spec/01-schema.md` (`tools/validate_spec.py:609`) but `sql` is absent from `_SCANNED_FENCE_LANGS` (`:632`, `{"text", "", "http", "yaml"}`), so the superseded-claims check never sees SQL comments. Whether that fence-language gap is itself worth closing is part of the same decision. The remaining repo instances of the plural are governed and should stand: `synthesis/reference-architecture.md:228` by the frozen "boundary wins" sentence, `decisions/ADR-0004-runtime-is-adapter-based.md:131` by the redo-4 amendment directly below it, and `synthesis/scope-reconciliation.md:224`/`:240` as the dated record of the decision itself. |
| OQ-050 | `tools/superseded-patterns.txt:18,21` state the two-SDK sequencing, superseded 2026-08-27, as live fact in their rationale column: "the first adapter is the Claude Agent SDK, not Claude Code" and "replaced by the Claude SDK adapter, then the OpenAI SDK adapter". Is that column normative prose that must be amended with a date like any other claim, or an operator note exempt by being outside the scan? | Round-2 review (consistency R3-03) | Satiya | 2026-09-15 | resolved 2026-08-27 — patterns rationale now names Pydantic AI | Last touched at 88ee4e4, before both rounds, so outside their scope. The patterns themselves are unaffected — only the human-readable third column is stale — and `tools/` is not in `_scanned_files()`, so the gate cannot police the reasons it prints. `decisions/ADR-0004-runtime-is-adapter-based.md:132` supersedes exactly this sequencing ("**One harness, on Pydantic AI 2.35.0**, replacing \"Claude Agent SDK then OpenAI Agents SDK\""), and OQ-043 records it as rescoped. |
| OQ-051 | The Postgres gate's new failure-cause heuristic (`tools/validate_spec.py:546`) takes the **last** qualifying unindented stderr line, but psycopg prints `DETAIL:` after the exception class, so the class is discarded again on the dirty-database failure `make check` actually hits. Should the heuristic prefer the first line matching `^\w[\w.]*Error\|^psycopg\.errors\.` and fall back to the current rule? | Round-2 review (scope-creep SC-10) | Satiya | 2026-09-15 | open | Observed at HEAD: `x postgres gate failed: DETAIL:  Key (tenant_id, run_id)=(1, r1) is still referenced from table "run_events". (did you apply spikes/03-postgres/schema.sql? see spikes/03-postgres/RESULT.md:74)`. Round 2's F4-gate fix is a real improvement over round 1's bare `x postgres gate failed` and works on the empty-database case it was tested against (`psycopg.errors.UndefinedTable: relation "approvals" does not exist`); it is the dirty-database case that still loses the class. Not fixed by the judge: nit severity, and changing the heuristic is verifier surface. Related to OQ-046 and OQ-047. |
| OQ-052 | Two counts in `check_counts()` are compared between hand-written numbers rather than against the artifact. (a) `spec_docs` is asserted only between `synthesis/scope.yaml` and `README.md`, so both can move together undetected. (b) `spike_assertions` is derived from each `RESULT.md`'s hand-written `**Gate assertions: N**` headline (`tools/validate_spec.py:764-782`), never from running the suite. Should either be derived from the artifact — and if so, does `contracts/` count toward `spec_docs`, and can the gate afford to run all four spikes to total them? | Round-1 review (consistency F2); closing judge, after (b) drew blood | Satiya | 2026-09-15 | open | (b) is not hypothetical: round 2's own F2 fix added a 43rd assertion to spike 03 and no number moved, and `make check` stayed green on the false count through two full review rounds. The closing judge measured it directly — `73fc21e` prints `42 passed, 0 failed`, `b4cef6e` prints `43 passed, 0 failed` — and corrected the four declared numbers (RESULT.md 42→43, README 116→117, scope.yaml 116→117) in the closing commit. The gate did not catch it and still would not. (a) was verified by mutation in round 1: moving `spec_docs` to 11 in both files leaves `counts vs source of truth` green. `scope.yaml:83-86`'s comment was corrected in round 2 to stop claiming otherwise. |
| OQ-053 | A document freeze is invisible to every tool. `tools/check_exit_criteria.py:136` reads only the literal `<!-- status: final -->`, so D4 and the deliverables table cannot distinguish a document still being edited from one closed on 2026-08-27; and frozen documents remain inside the superseded-claims scan, so a future scope change could make the gate demand an edit to a document the freeze says must not be edited. Should `frozen` become a parsed status, and should a superseded claim in a frozen file get a dated note rather than a rewrite? | Round-1 implementer and review (F4/SC-07, SC-01), round-2 implementer | Satiya | 2026-09-15 | open | Both halves were tried and backed out in the two rounds rather than decided. Round 1 added a second `<!-- status: frozen 2026-08-27 -->` marker and a `_FROZEN_FILES` scan exclusion; round 2 removed both — the marker because nothing parsed it, the exclusion because it measurably removed real coverage (with it in place, planted mutations in `synthesis/reference-architecture.md` and `synthesis/exit-criteria.md` were missed; with it gone they are caught, and both files are clean today). What remains is the freeze recorded only in dated prose. Teaching the parser a new status is genuine new surface and belongs in a decision, not a fix. |
| OQ-054 | Should `synthesis/scope.yaml` hold the harness choice (Pydantic AI 2.35.0) as an asserted scope value, and if so against which document — `v01-boundary.md` Tier 1 #4, ADR-0004, or both? | Round-1 implementer and review (SC-03), round-2 implementer | Satiya | 2026-09-15 | open | Round 1 added `harness` and `adapters_shipped` keys; round 2 deleted them, because `check_scope_source_of_truth()` reads only `tiers`, `deferred`, `never`, `counts` and `owed_specs` — so the file created to stop unpoliced scope claims was itself carrying two. Verified by mutation in both rounds: rewriting `harness` to `LangGraph 9.9.9` left the check silent. Deletion was the right resolution for the round, but the underlying question is unanswered: the harness is the single scope value this whole change propagated, and no machine assertion covers it anywhere. It is also the value OQ-043 says is a **placeholder until spike 06 fixes the pinned version** — *amended 2026-08-27: that half is now answered and only the scope-assertion half remains open. Spike 06 fixed the version by **digest**, not by declaration: nineteen SHA-256 digests *(amended 2026-08-28, superseding "sixteen": `models/__init__.py`, `profiles/__init__.py` and `capabilities/__init__.py` were added after review showed the admission filter could be neutered under the sixteen-file pin)* of the installed `pydantic_ai` files the gates depend on (`spikes/06-tool-interception/pinned_digests.json`), checked before collection. `pydantic-ai-slim == 2.35.0` is a label for those digests, because `uv-dynamic-versioning` + no git tags means the source tree cannot state its own version. The open question is unchanged: no check in `tools/` asserts the harness choice against any document.* |
| OQ-055 | Shared data plane with strong isolation (fleet-wide bin-packing) or per-customer VPC data plane (ref-arch D3; packing only within a customer)? Under the v0.1 provider (gVisor checkpoint-and-kill, zero idle cost) the saving no longer depends on cross-customer packing, so per-customer is the honest default. | Sandbox provider discussion 2026-08-27 | ADR-0016 | when a provider change is proposed | open | Decide only if a provider change is proposed; not a v0.1 blocker. Owner: Satiya. |

| OQ-056 | **`sdk_in_process` cannot mean "a function call" across a Go control plane and a Python harness.** `spec/07` says the in-process transport "collapses to a function call" and `integration_mode` is CHECK-restricted to `sdk_in_process`; the control plane is **Go** (ADR/scope) and the harness is **Pydantic AI**, i.e. Python. Spike 06 ran Python end to end, so it proved the tool boundary but says nothing about the process boundary. Which is it: a Python control-plane component, a Go plane driving a Python sidecar over the §07 frames (making the "deferred remote transport" the *shipped* one), or Go embedding CPython? | **SPIKE 06** (surfaced, not resolved) | ADR-0004, spec/07, spec/01 `integration_mode` | **Tier 1, before item 4** | **open — BLOCKING the adapter** | The three answers imply different failure modes, different deployment units and a different meaning for `integration_mode`. This is a genuine contradiction between two accepted decisions, not an unanswered design question, so per the 2026-08-27 classification rule it must be **corrected** — but the correction is a design choice, so it is filed as an OQ with an owner rather than patched silently. | — |
| OQ-057 | **The default dispatch route supplies its own evidence for the run-lease predicate.** `harness.py._lease_for` MINTS the `RunLease` that `claim()` is then checked against, and `dispatch` sets `owner = lease.holder` and `fence = lease.fence_token` from that same object — so on the default route `spec/02-consistency.md:246-248` predicate (b) holds by construction and can never fail. Every negative control that shows it refusing (no lease, expired, foreign holder, another run, superseded fence) injects `lease=` explicitly, so the predicate is verified only for a caller that volunteers a bad lease. What would make it load-bearing is a lease **read** from a store the claimant does not write, which is spike 03's territory. | Round-4 verifier (sdk-boundary lens), 2026-08-28 | Satiya | 2026-09-15 | open | Recorded as simplification 12 in `spikes/06-tool-interception/RESULT.md`, not fixed in round 4: fixing it means giving the harness a lease store, which is new scope. |
| OQ-058 | **`make check` runs the Postgres gate twice in one invocation, and the second run inherits the first's residue.** `tools/test_validate_spec.py` (Makefile:34) exercises the gate as a negative control and leaves a `runs` row behind; `tools/validate_spec.py` (Makefile:35) then runs it again against the same fixed DSN. Observed failures on a database carrying that state: `ForeignKeyViolation ... Key (tenant_id, run_id)=(1, r1) is still referenced from table "run_events"`, and separately `FAIL NC fixture: the k4 ledger row exists before the claim` + `status stays 'denied', never 'indeterminate'` + `an APPROVED effect can be claimed`. A freshly-schema'd database exits 0, so this is state-dependent, not a deterministic HEAD failure — but a gate whose verdict depends on residue from an earlier run in the same `make check` is the non-idempotence shape `VERIFICATION-RULES.md` already records for `schema.sql`. | Round-4 verifier (sdk-boundary + ledger-spec lenses), 2026-08-28 | Satiya | 2026-09-15 | open | Out of spike 06's lens: `spikes/06-tool-interception` is green standalone and inside `make check`. The fix is ownership of the Postgres fixture, in `tools/`. |
| OQ-059 | **`check_postgres_gate()` shares one fixed-name, mutually-destructive fixture across every nested run.** `spikes/03-postgres/test_postgres.py:44-56` `fresh()` DELETEs every tenant-1 row and re-creates run `r1`; `tools/test_validate_spec.py:58,189,207` runs a full `validate_spec.py` inside each repo copy with no `PHOENIX_PG_DSN` override, so every control re-runs that same gate against the same database, and leaked temp-copy `validate_spec.py` processes were observed still connected to `127.0.0.1:55433/spike` after their run. That is the ownership half of rule 7 and it makes the repository gate nondeterministic. Strengthens OQ-058. | Round-4 verifier (ledger-spec lens, out of lens), 2026-08-28 | Satiya | 2026-09-15 | open | Proposed fix direction: a per-run DSN or a uniquely-named database per nested invocation, so no two runs share a fixture. |
| OQ-060 | **`check_counts()` cannot see three documents that state spike-06 counts.** It compares `spike_assertions` only between each `RESULT.md` headline, `README.md` and `synthesis/scope.yaml` (`tools/validate_spec.py:764-782,903-904`). `spec/07-adapter-protocol.md:326`, `synthesis/v01-boundary.md:39` and `decisions/ADR-0016...:101` each state a spike-06 assertion count that **no check reads**, and all three were stale for three rounds (18 / 26 / 18 against an actual 47). Round 4 corrected them by hand — the fourth hand-correction of the same class. Strengthens OQ-052(b). | Round-4 verifier (hygiene lens, out of lens), 2026-08-28 | Satiya | 2026-09-15 | open | Proposed fix direction: have `check_counts()` scan every tracked `.md` for a spike-06 assertion count and compare each against `RESULT.md`, rather than enumerating three known sites. |
| OQ-061 | **Two counts inside `spikes/06-tool-interception/RESULT.md` are still hand-kept prose.** The mutation table's per-mutation "gates broken" figures and the pin's file count are regenerated by hand from `mutate.py` / `verify_pin.py` output. `mutate.py` GENERATES the table (round 3's fix) but nothing asserts the pasted copy matches a fresh run, which is exactly how the round-2 table went stale (3 claimed, 6 measured). | Round-4 implementer, 2026-08-28 | Satiya | 2026-09-15 | open | Proposed fix direction: `make spike06-mutations` writes the table to a file and a gate diffs it against `RESULT.md`, or the numbers move out of prose entirely. |
| ID | Deferred | Why | Revisit when |
|---|---|---|---|
| | | | |
