# Superseded claims. One entry per line, TAB-separated:
#     REGEX <TAB> DATE <TAB> REASON
#
# Matching is CASE-INSENSITIVE and applied PER SENTENCE (split on ". ", newline,
# and "|"), not per line. Both were faults in the previous version: it missed
# "Our adapter exposes four methods." because the pattern was `Four methods`, and
# it missed "We adapt agents supplied by customers." because the pattern was
# `adapt.*someone else`.
#
# A sentence is exempt ONLY if it carries a dated marker (20\d\d-\d\d-\d\d) AND one
# of superseded|amended|reversed|retracted, or it lives in
# synthesis/scope-reconciliation.md (the change log) or a projects/ teardown
# (evidence about another system is not a claim about ours).
#
# QUOTING IS NOT AN EXEMPTION. "This once said X" must also carry the dated marker.

# --- SaaS repositioning: we build and operate the agents (2026-08-27) ---
claude code	2026-08-27	the first adapter is the Claude Agent SDK, not Claude Code
\bcodex\b	2026-08-27	no foreign-agent path; there is no Codex adapter
acp_subprocess	2026-08-27	reserved in the enum, not shipped; sdk_in_process only
acp adapter	2026-08-27	replaced by the Claude SDK adapter, then the OpenAI SDK adapter
adapter \(acp\)	2026-08-27	no ACP adapter ships
# Bare ACP as a SHIPPED path. Anchored so it does not flag ADR-0006 discussing ACP
# as a protocol, or a teardown noting that another project speaks it.
# "ACP ... ships/first/day one" only. NOT `acp sitting behind an adapter`, which is
# ADR-0006's still-correct statement that ACP is a protocol we may speak, not a
# transport we adopt.
\bacp\b[^.]{0,30}\b(ships?|ship first|day one)\b	2026-08-27	ACP is not a shipped path; sdk_in_process only
\b(the )?acp path\b	2026-08-27	there is no ACP path in v0.1
native_tui	2026-08-27	reserved in the enum, not shipped
bring[- ]your[- ]own	2026-08-27	customers bring knowledge, connectors and data, never agent code
adapt(s|ing|ed)?\b.{0,60}\bagents?\b	2026-08-27	we build and operate the agents; we do not adapt anyone's
someone else'?s agents?	2026-08-27	running others' agents is no longer a requirement
other people'?s agents?	2026-08-27	running others' agents is no longer a requirement
framework[- ]neutral	2026-08-27	the thesis is a SaaS platform where we build and operate the agents
acp:	2026-08-27	adapter identities are `sdk:<vendor>`; there is no acp: identity
claude-code	2026-08-27	the first adapter is the Claude Agent SDK; `claude-code` is not an identity we ship
acp covers the agents we care about	2026-08-27	ACP is not a shipped path; the reason is now SaaS/SDK-only
no task resource yet	2026-08-27	Task/routines are Tier 2; `runs.task_id` arrives in spec 10/11
tells? (you )?whether the effect happened	2026-08-27	overclaim: it knows which effects are SETTLED and which are UNCERTAIN
can tell you whether the effect happened	2026-08-27	overclaim: it knows which are settled and which are uncertain
the restriction costs nothing	2026-08-27	the cost is real: vendor-hosted tools are unsupported until spike 06
spike the storage swap before committing	2026-08-26	done: spikes/01-ag2-storage
assigned to it as approver	2026-08-27	no assignee model exists; agents do not decide approvals in v0.1
four[- ]methods?	2026-08-27	two RPCs; AX's four-method contract is upstream precedent, not our shape
start → \{run, send, cancel, close\}	2026-08-27	the adapter surface is Run and Describe
\btask\b.{0,40}\bdefer	2026-08-27	Task/routines moved to Tier 2; the deferral trigger is met
\bdefer.{0,40}\btask\b	2026-08-27	Task/routines moved to Tier 2; the deferral trigger is met

# --- unmediated tools RETRACTED (2026-08-27, redo 2) ---
cost is accepted	2026-08-27	the paragraph was retracted, not refined
platform_executed\s*=\s*false	2026-08-27	unmediated tools are UNSUPPORTED in v0.1; spike 06 decides
unmediated	2026-08-27	retracted: the platform cannot keep an `observed` guarantee across a vendor boundary

# --- effect lifecycle wording ---
# `pending` is CORRECT for approvals (approvals.status has it) and WRONG for the
# effect ledger (where the status is `claimed`). Anchored to the ledger senses only:
# a pending row that is described as an effect, or as expiring against a lease.
\bpending\b (ledger )?row.{0,40}(lease|effect|indeterminate)	2026-08-27	the ledger status is `claimed`; `pending` belongs to approvals
effect leaves a `?pending`? row	2026-08-27	the ledger status is `claimed`
two pending rows for one effect	2026-08-27	the ledger status is `claimed`
one gets a unique violation and rolls back	2026-08-27	spike 03: the loser BLOCKS, then raises 23505 after the winner commits
answer its own approvals	2026-08-27	agents do not decide approvals in v0.1 (D-A); there is no assignee model and no self-approval rule
third[- ]party adapter	2026-08-27	no third party writes adapters for us; the contract exists so a SECOND adapter is implementable
# A wrong CITATION is a defect the gate can catch, same as a wrong claim. :2399 is an
# @overload stub; the tool decorator's implementation is :2423 and usage :2460.
__init__\.py:2399	2026-08-27	miscitation: :2399 is an @overload stub, not the @agent.tool implementation (:2423)
30 provider modules	2026-08-27	miscitation: a file count of models/*.py; there are 16 provider model modules
# The overbroad verification rule, corrected the day it was written. Fixtures may
# delete state they own; the rule is about OWNERSHIP, not the SQL verb.
no destructive statement in it at all	2026-08-27	overbroad: a verifier must not destructively mutate state it does not UNIQUELY OWN

# --- counts that drifted ---
all 15 adrs are accepted	2026-08-27	16 ADRs: 15 Accepted, 1 Proposed
# `15 accepted` alone over-matches the CORRECT phrasing "16 ADRs (15 Accepted, 1
# Proposed)", so it is anchored to the claim that 15 is the TOTAL.
(the |holds the |all )15 accepted adrs	2026-08-27	16 ADRs: 15 Accepted, 1 Proposed
\b15 adrs\b	2026-08-27	there are 16 ADRs: 15 Accepted, 1 Proposed

# --- earlier reversals ---
sorted by \*\*utf-16 code unit\*\*	2026-08-26	UTF-16 ordering replaced by UTF-8 byte order
nfc\+jcs	2026-08-26	profile renamed nfc+intjson; it is not JCS
approvalrequest\s+approval_request\s*=	2026-08-27	the frame was removed; the platform decides on approval
create table effect_claim_history	2026-08-27	effect rows are never reclaimed; monotonic claim tokens are meaningless
"trace_id":	2026-08-27	the log stores a full W3C traceparent, not a bare trace id
