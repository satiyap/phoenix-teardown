# Superseded claims. One regex per line: REGEX <TAB> DATE <TAB> REASON
#
# A hit FAILS the build unless the matching line also contains an amendment
# marker (superseded | amended | a (YYYY-MM-DD) date) or sits in a table row of
# scope-reconciliation.md, which is the change log and must quote what changed.
#
# This file exists because the previous hard-coded list scanned only spec/*.md
# and passed while five other files contradicted the 2026-08-27 repositioning.
#
# THREE THINGS ARE NOT DRIFT, and the tool must not flag them:
#
#   1. Evidence about ANOTHER project. `projects/*/teardown.md` and the ADR evidence
#      logs record what LangGraph or OpenHands does; "OpenHands launches Claude Code"
#      is a true statement about OpenHands and always will be. Excluded by directory
#      (projects/) and by the evidence-table shape (a `| ... |` row).
#   2. Probe and matrix text. `capability-matrix.md` and `recon.md` name foreign agents
#      because the PROBE asks about them. Excluded by file.
#   3. Retained enum values. `acp_subprocess` and `native_tui` stay in the CHECK
#      constraint so the column never needs a migration; the restriction is a separate
#      constraint. Excluded when the line is inside a SQL string literal.
#
# What IS drift: a statement in OUR voice about what WE build or ship.

# --- SaaS repositioning: we build and operate the agents (2026-08-27) ---
Claude Code	2026-08-27	we build the agents; the first adapter is the Claude Agent SDK, not Claude Code
Codex	2026-08-27	no foreign-agent path; there is no Codex adapter
acp_subprocess	2026-08-27	reserved in the enum, not shipped; sdk_in_process only
ACP adapter	2026-08-27	replaced by the Claude SDK adapter, then the OpenAI SDK adapter
native_tui	2026-08-27	reserved in the enum, not shipped
bring.your.own	2026-08-27	customers bring knowledge, connectors and data, never agent code
adapt.*someone else	2026-08-27	we do not adapt third-party agents
framework-neutral	2026-08-27	the thesis is a SaaS platform where we build and operate the agents
other people's agents	2026-08-27	running others' agents is no longer a requirement
Task.{0,40}defer	2026-08-27	Task/routines moved to Tier 2; the deferral trigger is met
defer.{0,40}Task	2026-08-27	Task/routines moved to Tier 2; the deferral trigger is met
cost is accepted deliberately	2026-08-27	replaced by the narrow unmediated-tools permission in spec/07
Four methods	2026-08-27	two RPCs; AX's four-method contract is upstream precedent, not our shape

# --- earlier reversals, previously hard-coded in validate_spec.py ---
sorted by \*\*UTF-16 code unit\*\*	2026-08-26	UTF-16 ordering replaced by UTF-8 byte order
nfc\+jcs	2026-08-26	profile renamed nfc+intjson; it is not JCS
they get\s*\n?`INCOMPATIBLE`	2026-08-27	repointing an agent no longer blocks in-flight runs
ApprovalRequest\s+approval_request\s*=	2026-08-27	the frame was removed; the platform decides on approval
The worker delivers `ToolResult\{ok:false	2026-08-27	denial is delivered as ToolDenied
claimed, by unique-index\s*\n?\s*insert	2026-08-27	the claim is a conditional UPDATE; only intent is an insert
CREATE TABLE effect_claim_history	2026-08-27	effect rows are never reclaimed; monotonic claim tokens are meaningless
answer its own approvals	2026-08-27	a principal may never decide an approval gating its own effect
one gets a unique violation and rolls back	2026-08-27	spike 03: the loser BLOCKS, then raises 23505 after the winner commits
"trace_id":	2026-08-27	the log stores a full W3C traceparent, not a bare trace id
